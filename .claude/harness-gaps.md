# harness gaps — 발생 기록

`CLAUDE.md` §5 의 "두 번 이상 발생하면 제안한다" 를 세기 위한 파일이다. 세션을
넘겨 발생을 세는 장치가 이것뿐이므로, **1회차도 적는다** — 1회차를 안 적으면
2회차가 영원히 1회차로 보인다.

한 항목: 날짜 · 파일:줄 · 무슨 일이 있었나 · 몇 회차인가. 2회차가 되면 그때
PR 본문 `## Notes` 로 올라가고, 고치는 것은 그다음이다.

---

## 2026-08-07 — `superpowers:finishing-a-development-branch` 가 위임을 받지 않는다

- **어디**: `docs/adr/0009-external-dependencies.md` — 갱신 절의 위임 판정
- **무슨 일**: ADR-0009 는 머지·브랜치 정리를 Superpowers 쪽에 남기고 우리는
  PR 생성까지만 한다고 정했다. 실측에서 "이 PR 머지하고 브랜치 정리해줘. squash
  로 넣고 원격 브랜치도 지워줘" 가 **세 번 다 `Bash` 로** 갔다. 위임한 상대가
  그 자리를 받지 않는다. 사용자는 규약 없는 맨 `git` 명령을 받게 된다.
- **왜 지금 안 고치나**: 1회차다. 그리고 고치는 방법이 둘인데 어느 쪽인지
  모른다 — 우리가 `pr-finish` 를 만들어 자리를 메우거나, 위임 자체를 철회하고
  `pr-create` 의 범위를 넓히거나. 둘 다 되돌리기 비싼 결정이라 두 번째 발생을
  기다린다.
- **2026-08-08 분류**: 대기 — 그리고 반대 증거가 하나 생겼다. 이번 세션에서
  "머지하고 retro" 에 이 스킬을 Skill 로 불렀고 정상 로드됐다. 원 관측은 *위임이
  Bash 로 샜다* 였는데, 명시적으로 부르면 동작한다. 2회차 조건을 "부르지 않았는데
  샜다" 로 좁힌다.
- **회차**: 1

## 2026-08-07 — 스킬 본문의 경로 오타는 조용히 죽는다

- **어디**: `plugins/harness-core/skills/pr-create/SKILL.md:90`
- **무슨 일**: Step 6 을 쓰면서 `.claude/harness-gaps.md` 를 `.claece/...` 로
  적었다. `[ -f ... ] && ...` 형태라 파일이 없으면 조용히 아무 일도 안 하고
  넘어간다 — 즉 **이 절이 영원히 안 도는데 아무도 모른다.** 같은 회 안에서
  발견해 고쳤다.
- **일반화**: 훅은 `verify-*.sh` 가 지키고 프론트매터는 `verify-frontmatter.sh`
  가 지키는데, **스킬 본문 안의 경로·명령은 아무도 안 본다.** `check-claims.sh`
  가 덱의 수치를 검사하듯, 스킬이 언급하는 경로가 실재하는지 훑는 검사가 같은
  종류의 빈틈을 덮는다.
- **왜 지금 안 고치나**: 1회차. 다만 이건 값이 싼 검사라 2회차가 나오면 바로
  만든다.
- **2026-08-08 분류**: 2회차가 아래 *"문서가 가리키는 내부 참조는 아무도 안 본다"*
  로 섰고 `scripts/verify-doc-refs.sh` 로 해결됐다. 이 항목은 그 1회차이므로
  더 셀 것이 없다.
- **회차**: 1 (2회차에서 해결됨)

## 2026-08-07 — R1-3 예외 조항이 "커밋 보류" 를 말하는데 커밋된다

- **어디**: `plugins/harness-core/declarative/rules/core/workflow.md:20` (R1-3)
- **무슨 일**: 규칙은 *"단일 파일 typo · 한 줄 수정 · 임시 탐색은 PR 단위가
  아니다. 결과만 보고하고 commit·push 는 보류"* 라고 적혀 있다. 한 줄 수정
  과제를 준 벤치마크 3회에서 에이전트는 **브랜치는 안 팠지만(예외 준수) 커밋은
  했다(예외 위반)**. 예외의 앞 절반만 지켜진 셈이다.
- **왜 그럴 수 있나**: 같은 절의 불변량이 "`main` 직접 **push** 금지" 로 두 번
  다 *push* 를 말한다. commit 만 하는 요청에는 걸리지 않는 문구다.
- **왜 지금 안 고치나**: 1회차이고, 관측 조건이 "커밋해줘" 라고 명시적으로
  지시한 경우였다 — 사용자 지시가 규칙의 보류 권고를 이긴 것이라면 정상 동작에
  가깝다. 지시 없이도 커밋하는 사례가 나오면 2회차로 센다.
- **2026-08-08 분류**: 대기. 재발 없음.
- **회차**: 1

## 2026-08-07 — `ask` 티어가 헤드리스에서 PR 플로우를 도달 불가로 만든다

- **어디**: `plugins/harness-core/declarative/settings-fragment.json` — `permissions.ask` 의 `Bash(git push:*)`
- **무슨 일**: 비대화형 `claude -p` 세션은 권한 프롬프트에 답할 수 없어 push 가
  항상 거부된다. `acceptEdits` · `dontAsk` · `bypassPermissions` 세 모드 모두에서
  재현했다. `pr-create` 스킬은 Step 5 가 push 라서 헤드리스에서는 **완주가
  구조적으로 불가능**하다.
- **왜 지금 안 고치나**: 1회차이고, `ask` 티어 자체는 의도된 설계다 (되돌리기
  어려운 동작은 사람이 승인한다). 답이 자명하지 않다 — 헤드리스를 감지해
  완화할지, `pr-create` 가 push 직전에 멈추고 명령을 내어줄지, 아니면 문서로만
  알릴지. 2회차가 나오면 셋 중 하나를 고른다.
- **2026-08-08 분류**: 대기. 재발 없음 — 이번 세션의 헤드리스 실행(벤치 72세션)은
  push 를 하지 않아 이 경계에 닿지 않았다.
- **회차**: 1

## 2026-08-07 — `--with` 를 반복하면 앞엣것이 조용히 사라졌다

- **어디**: `plugins/harness-core/bin/harnessctl:54` (인자 파싱)
- **무슨 일**: 사용법은 `--with dev,research` 인데, `--with dev --with research`
  라고 쓰면 뒤엣것이 앞엣것을 덮어써서 **사용자가 명시적으로 요청한 모듈이 경고
  없이 빠졌다.** 문서 정리 중 실제로 이 형태로 쳤다가 발견했다.
- **1회차인데 왜 바로 고쳤나**: 이건 후보 기능이 아니라 **조용한 오동작** 이다.
  §7 의 "두 번 발생을 기다린다" 는 새 규칙·새 훅을 늘릴 때의 기준이지, 이미
  배포된 코드가 요청을 말없이 무시하는 경우에 적용할 기준이 아니다. 반복 시
  누적하도록 고치고 `verify-install.sh` 에 회귀 케이스 둘을 박았다 (90 → 92).
- **회차**: 1 (해결됨)

## 2026-08-07 — 시크릿 픽스처가 GitHub push protection 에 걸린다

- **어디**: `evals/incidents.sh` · `plugins/harness-core/scripts/verify-secret-scrubber.sh`
- **무슨 일**: 저장소를 public 으로 올리는 첫 푸시가 거부됐다. GitHub 이 우리
  Slack 토큰 **픽스처** 를 진짜 시크릿으로 판정했다. 시크릿 탐지기를 시험하려면
  시크릿처럼 생긴 문자열이 있어야 하는데, 그게 정확히 스캐너가 찾는 것이다.
- **고친 방법**: 우리 정규식(`xox[abprs]-` + 20자)에는 걸리고 GitHub 의 구조
  판정에는 안 걸리는 값으로 교체. 훅 차단(exit 2)과 벤치마크 27/29 는 그대로다.
- **일반화**: 새 시크릿 패턴을 추가할 때마다 픽스처가 실제 스캐너에 걸리는지
  확인해야 한다. private 일 때는 드러나지 않는 종류의 문제다.
- **회차**: 1 (해결됨)

## 2026-08-07 — 플러그인 `bin/` 은 사용자 셸에서 안 보인다

- **어디**: `install.sh` · `README.md` (설치 안내)
- **무슨 일**: 사용자가 터미널(zsh)에서 `harnessctl` 을 쳤는데 없었다. 플러그인
  `bin/` 은 Claude Code **Bash 도구** 의 PATH 에만 올라간다. README 는 그냥
  "PATH 에 자동으로 오른다" 고만 적어 두어 사용자가 터미널에서 칠 수 있는 것처럼
  읽혔다 — 설치가 깨진 것처럼 보인다.
- **왜 심링크로 못 푸나**: 캐시가 버전별 디렉터리(`harness-core/1.6.0/bin/`)라
  업데이트마다 경로가 바뀐다. 지금 거는 심링크는 다음 업데이트에 죽는다.
- **고친 방법**: `~/.local/bin/harnessctl` 에 실행 시점 해석 shim. 평범한
  실행 파일이라 셸 무관하고, `sort -V` 로 최신 버전을 고르므로 업데이트를 따라간다.
  PATH 에 없으면 `$SHELL` 을 보고 맞는 rc 파일과 한 줄을 안내한다.
- **1회차인데 왜 바로 고쳤나**: 사용자가 첫 명령에서 막혔다. 설치기가 "완료" 라고
  말한 뒤 안내한 명령이 동작하지 않는 것은 후보 기능이 아니라 결함이다.
- **회차**: 1 (해결됨)

## 2026-08-07 — 벤치가 모델·effort 를 상속하고 기록하지 않았다

- **어디**: `scripts/bench-{convention,lsp}.sh` · `scripts/bench-trigger.py`
- **무슨 일**: `claude -p` 에 `--model`·`--effort` 를 안 넘겨 호출자의
  `settings.json` 을 그대로 상속했다. 실제로는 `opus[1m]` / `effort=high` 였는데
  **어디에도 기록되지 않았다.** 다른 머신에서 돌리면 다른 설정으로 다른 숫자가
  나오는데 왜 다른지 알 방법이 없다.
- **왜 중요한가**: effort 는 결과를 움직일 개연성이 있다. 추론을 더 하면 하네스
  없이도 규약을 지킬 수 있고, 그러면 raw 팔이 올라가 효과가 작게 나온다. 즉 이
  값을 안 적으면 측정의 유효 범위 자체가 불명확하다.
- **고친 방법**: `_bench-lib.sh` 가 `BENCH_MODEL`(기본 opus)·`BENCH_EFFORT`(기본
  high)를 정의하고 세 벤치가 전부 넘긴다. 시작할 때 출력하고 문서에도 명시했다.
- **1회차인데 왜 바로 고쳤나**: 기록되지 않은 설정은 이미 발표된 수치의 유효
  범위를 불명확하게 만든다. 문서가 거짓을 말하는 상태에 가깝다.
- **회차**: 1 (해결됨)

## 2026-08-07 — `verify-benches` 가 코퍼스를 실제보다 작게 센다

- **어디**: `scripts/verify-benches.sh:36`
- **무슨 일**: 게이트가 `grep -c '^inc '` 로 세어 **48** 을 출력하는데, 코퍼스는
  53 이다 (`inc_tool` 5줄이 접두사 `inc ` 에 안 걸린다). `bench-guards.sh` 자신은
  "corpus: 53 cases" 라고 출력하므로 **같은 코퍼스를 두 스크립트가 다르게 센다.**
  `>= 40` 게이트는 통과하니 아무것도 실패하지 않는다.
- **왜 신경 쓰나**: §4b 의 "계측기를 여섯 번 틀렸다" 와 같은 부류다 — 계기가
  조용히 어긋나 있고 화면에는 PASS 로 보인다. 지금은 여유가 커서 무해하지만,
  `inc_tool` 케이스만 늘리면 게이트가 실제 크기와 무관해진다.
- **고친 방법**: `grep -cE '^inc(_tool)? '`. 이제 게이트와 벤치가 같은 53 을
  말한다. 사용자가 같은 턴에 승인해서 바로 고쳤다 — 새 규칙을 늘리는 것이
  아니라 이미 있는 계기가 어긋난 경우라 §7 의 "두 번째 발생을 기다린다" 대상이
  아니다.
- **회차**: 1 (해결됨)

## 2026-08-07 — 문서가 가리키는 내부 참조는 아무도 안 본다 (2회차)

- **어디**: `README.md` — 새로 쓴 `[근거와 방법은 아래](#2-make-bench----날것-…)`
- **무슨 일**: 절 앵커를 손으로 추정해서 적었고 틀렸다. GitHub 의 slug 규칙은
  공백을 접지 않아서 em dash 자리에 하이픈이 둘 남는데 나는 넷을 적었다.
  링크는 렌더링되고, 눌러야만 아무 데도 안 간다는 걸 안다. 임시로 짠 python
  검사기가 잡았고, **그 검사기는 저장소에 남아 있지 않다** — 이번 세션에서
  같은 걸 세 번 다시 썼다.
- **왜 2회차인가**: 위의 `pr-create/SKILL.md:90` 경로 오타(`.claece/`) 와 같은
  부류다. 그 항목이 이미 일반화를 적어 두었다 — *"훅은 verify-*.sh 가 지키고
  프론트매터는 verify-frontmatter.sh 가 지키는데, 본문 안의 경로·명령은 아무도
  안 본다."* 1회차는 스킬 본문의 파일 경로, 2회차는 README 의 절 앵커. 둘 다
  깨져도 에러가 아니라 **아무 일도 안 일어나는 것** 으로 나타난다.
- **R3 criterion**: ≥2회 ✅ · project-agnostic ✅ (자기 파일과 자기 절을 가리키는
  문서는 어느 저장소에나 있다)
- **고친 방법**: `scripts/verify-doc-refs.sh` — 링크 대상 파일 존재, `#anchor` 의
  heading 해석(GitHub slug 규칙), instruction 파일이 부르는 경로의 첫 세그먼트
  존재. `make verify` 에 편입. 자기 케이스 19개를 먼저 돌린다.
- **첫 실행이 3회차를 바로 찾았다**: `pr-review` 와 `research-notes` 본문이
  체크리스트를 `rules/harness/…` 로 가리키고 있었다 (`pr-create` 는 같은 자리를
  `.claude/rules/harness/…` 로 적는다). 프로젝트 루트에서 해석 안 되는 경로다.
- **그리고 오탐 셋을 먼저 냈다** — 동결 코퍼스, `declarative/` 페이로드의
  설치 후 기준 상대 링크, 그리고 **이 기록이 깨진 링크를 증거로 인용한 것**.
  셋 다 검사기를 고쳐서 없앴다. 문서를 고치는 쪽으로 갔으면 맞는 것을 틀리게
  만들었을 것이다.
- **회차**: 2 (해결됨)

## 2026-08-07 — §6 의 "별도 PR" 에 임계값이 없다

- **어디**: `CLAUDE.md` §6 — *"하네스 자체의 구조 변경과 콘텐츠 추가는 별도 PR 로"*
- **무슨 일**: 이번 PR 에 README 개편(문서)과 `verify-benches.sh` 4줄 수정(계기)이
  같이 들어갔다. 문자 그대로 읽으면 PR 둘이어야 하는데, 4줄짜리 게이트 수정에
  PR 을 따로 여는 것은 규칙이 막으려던 문제(리뷰어가 둘 다 제대로 못 본다)를
  일으키지 않는다. 커밋 분리로 갈음하고 PR 본문에 왜 그랬는지 적었다.
- **무엇이 빠졌나**: 규칙에 "언제 커밋 분리로 충분한가" 가 없다. 판단을 매번
  즉석에서 한다.
- **왜 지금 안 고치나**: 1회차. 그리고 임계값을 잘못 적으면 규칙이 오히려
  느슨해진다 — 두 번째 사례를 보고 실제 경계가 어디였는지 확인하고 적는다.
- **2026-08-08 분류**: 대기. 이번 W1 PR 은 스크립트+Makefile+문서를 한 PR 에 담았지만
  **한 작업단위 안의 산출물** 이라 §6 이 막으려는 "구조 변경과 콘텐츠 추가의 혼합" 이
  아니다. 2회차로 세지 않는다.
- **회차**: 1

## 2026-08-07 — verify 총계가 문서 세 곳에 손으로 박혀 있다

- **어디**: `README.md` (배지 · 검증 표 합계) · `docs/agent-layer.md` §4 "현재" 문장
- **무슨 일**: `verify-doc-refs` 를 더하면서 합계를 356 → 425 로 바꿨는데, 그
  숫자가 **세 군데** 에 하드코딩돼 있어 셋을 손으로 맞췄다. 하나만 놓쳤어도
  문서는 조용히 거짓이 되고, `make verify` 는 여전히 통과한다.
- **닮은 사건이 이미 있다**: §4b 의 *"`bench-claims` 가 조용히 썩었는데 README 는
  마지막 수치를 현재형으로 인용하고 있었다"*. 원인은 다르다 (그건 벤치가 빈
  결과를 냈고, 이건 복제된 상수다) — 같은 항목으로 세지 않는다. 다만 이 저장소가
  `check-claims.sh` 로 **슬라이드** 에서 막는 바로 그 문제를 자기 README 에서는
  안 막고 있다는 점은 적어 둔다.
- **왜 지금 안 고치나**: 1회차. 그리고 같은 세션에서 검사기를 하나 막 만들었다 —
  1회차에 검사기를 또 붙이는 것이 §7 이 경고하는 "혹시 몰라서" 그 자체다.
  2회차가 나오면 `make verify` 의 실제 합과 문서의 숫자를 대조하는 검사가 답이다.
- **2026-08-08 — 2회차 발생.** `context-budget` 을 더하면서 425 → 426 을 **또 세
  군데에 손으로** 박았다 (README 배지 · README 표 · agent-layer §4). 예고한 조건이
  그대로 왔다. 아이러니는 같은 PR 이 *컨텍스트* 총계는 손 계산에서 떼어냈다는
  것이다 — **검사 총계는 그대로 뒀다.**
- **제안**: `make verify` 가 실제로 낸 합과 문서에 적힌 숫자를 대조하는 검사.
  `context-budget.sh` 와 같은 모양이고, 세 자리를 한 자리로 줄인다.
  **하네스 변경이므로 별도 PR** (§6).
- **2026-08-08 — 3회차.** 문서를 하나 더하자 `verify-doc-refs` 의 파일 수가 50 → 51
  이 되어 426 → 427 을 **또 세 군데에 손으로** 박았다. 이제 이 항목은 제안이 아니라
  밀린 일이다.
- **2026-08-08 — 4회차.** ADR-0012 를 더하자 427 → 428 을 또 세 군데에서 손으로
  고쳤다. 네 번이면 더 셀 이유가 없다.
- **고친 방법**: `scripts/verify-check-total.sh` + `make verify-all`. 두 가지를
  차례로 묻는다 — 발행된 셋이 서로 맞는가, 그리고 그 숫자가 **실제 실행 결과와**
  맞는가. 계수 규칙을 스크립트 헤더에 적어 두어 반박할 수 있게 했다. CI 의 ubuntu
  job 이 `verify` 대신 이것을 부른다.
- **2026-08-13 잔여 관찰**: 검사가 고정하는 것은 *총계*뿐이라, README 표의
  행 하나("installer round trip **151**")가 실제(159)와 8 어긋난 채로 총계만
  맞아 살아 있었다. jq bootstrap 8케이스가 더해질 때 행이 안 따라온 것.
  166 갱신 때 발견해 고쳤다. 행 단위까지 검사로 고정할지는 — 행 구성이
  바뀔 때마다 검사가 깨지는 비용과 맞바꾸는 것이라 — 5회차가 나오면 그때.
- **회차**: 4 (해결됨)

## 2026-08-07 — 저장소 전용 검증기의 케이스 의무가 규정에 없다

- **어디**: `CLAUDE.md` §4 (검증 의무) · `docs/agent-layer.md` §4
- **무슨 일**: §4 는 **훅** 에 "검증기 하나, 8케이스 이상, 세 종류" 를 요구하고
  스킬에는 트리거 eval 을 요구한다. 그런데 `scripts/` 의 저장소 전용 검증기에
  대한 규정은 없다 — `verify-frontmatter.sh` 는 자기 케이스가 없고,
  `verify-doc-refs.sh` 는 19개를 넣었다. 둘 다 규정을 어기지 않았다는 뜻이다.
- **판단 근거는 있었다**: doc-refs 의 유일한 실패 방식이 오탐이고, 오탐 내는
  린터는 꺼진다. frontmatter 는 참/거짓이 명확하다. 즉 "케이스가 필요한가" 는
  검증기의 실패 방식이 정하는데, 그 기준이 어디에도 안 적혀 있다.
- **왜 지금 안 고치나**: 1회차이고, 규정을 먼저 적으면 두 사례만 보고 일반화하는
  것이 된다. 세 번째 저장소 전용 검증기를 만들 때 실제 경계를 보고 적는다.
- **2026-08-08 — 세 번째가 생겼다.** `scripts/context-budget.sh`. 또 즉석에서
  판단했고, 자기 케이스를 넣지 않았다. 근거는 있었다 — 실패 방식이 *오탐* 이
  아니라 *누락* 이고, 파일 목록을 하드코딩 대신 glob 으로 두면 누락이 구조적으로
  불가능해진다. 즉 **케이스 대신 설계로 막았다.**
- **세 사례가 만든 기준** (이제 적을 수 있다): 검증기의 실패 방식이
  **오탐**이면 케이스를 넣는다 (오탐 내는 검사는 꺼진다 — `verify-doc-refs`).
  **누락**이면 먼저 설계로 막고, 못 막을 때만 케이스를 넣는다 (`context-budget`
  의 glob). 참/거짓이 자명하면 둘 다 불필요하다 (`verify-frontmatter`).
- **고친 방법**: 루트 `CLAUDE.md` §4 와 `agent-layer` §4 에 표로 넣었다.
  **컨슈머용 `CLAUDE.md` 에는 넣지 않았다** — 컨슈머는 우리 검증기를 쓰지 않으므로
  상시 컨텍스트 비용이 0 이다.
- **네 번째 사례가 기준을 하나 더 붙였다**: `verify-check-total` 이 CLI 없는
  환경에서만 도는 분기를 갖고 있었고 CI 에서 처음 실행되며 깨졌다. **환경 중
  하나에서만 도는 줄은 미검증이다** 를 같이 적었다.
- **회차**: 2 (해결됨)

## 2026-08-07 — 스킬과 그 SOT 가 서로 다른 스코프로 배포된다

- **어디**: `install.sh` (기본 `--scope user`) · `plugins/harness-core/bin/harnessctl:207`
  (user 스코프에서 `rules/` 를 건너뜀) · `plugins/harness-dev/skills/pr-review/SKILL.md:50`
- **무슨 일**: `pr-review` Step 4 는 *"`review.md` R2 의 6 항목을 순서대로
  걸어간다"* 라고 쓴다 — 체크리스트가 스킬 본문에 없고 rule 파일에 있다. 그런데
  **rule 은 프로젝트 스코프에만 설치되고, 설치기의 기본 스코프는 user 다.**
  즉 README 가 "가장 흔한 조합" 으로 안내하는 `./install.sh --profile dev,python`
  을 그대로 따르면 스킬은 전역에 깔리고 그 SOT 는 아무 데도 안 깔린다.
- **이 머신에서 확인**: `~/.claude/rules/` 없음. `harness-dev` · `harness-research`
  플러그인은 user 스코프로 설치돼 스킬이 살아 있는데, `review.md` 와 `notes.md`
  는 이 머신 어디에도 없다.
- **왜 설계가 그런가는 안다**: `~/.claude/rules` 는 읽히는 자리가 아니라서 거기
  두면 "동작하는 얼굴로 죽어 있는 파일" 이 된다 (harnessctl 주석). 문제는 그
  판단이 옳다는 것이 아니라, **그 결과로 생기는 구멍을 아무도 말해주지 않는다** 는
  것이다. `doctor` 는 "이상 없음" 이라고 말한다.
- **가장 싼 답부터**: `doctor` 가 "dev/research 플러그인이 있는데 대응 rule 이
  대상 스코프에 없다" 를 한 줄로 알리는 것. 스킬 본문에 체크리스트를 복제하는
  것은 SOT 두 벌이 되므로 (ADR-0004) 답이 아니다.
- **왜 지금 안 고치나**: 1회차. 다만 이건 후보 기능이 아니라 **안내가 침묵하는
  구멍** 이라, 2회차가 나오면 바로 `doctor` 에 넣는다.
- **2026-08-08 분류**: 대기. 재발 없음 — 다만 이번에 컨텍스트 예산을 재면서
  `rules/` 가 프로젝트 스코프 전용이라는 사실이 §4 표와 README 에 수치로
  드러났으므로, 구멍의 크기는 이제 문서화돼 있다 (user 3.9k 대 project 8.0k).
- **회차**: 1

## 2026-08-08 — PR 제목 70자 상한을 아무것도 재지 않는다 (2회차)

- **어디**: `plugins/harness-core/declarative/rules/core/workflow.md` R4 체크리스트 ·
  `plugins/harness-core/skills/pr-create/SKILL.md` Step 5
- **무슨 일**: R4 는 *"PR title 이 `[<slug>] <description>` 형식이고 70자 이하"* 를
  체크 항목으로 두는데, **재는 것이 없다.** 사람도 에이전트도 눈으로 센다.
  - **1회차**: PR #4 `[bench-trigger-project-skills] Measure skills…` — **71자**로
    나갔고 **그대로 머지됐다.** 아무도 몰랐다.
  - **2회차**: PR #6 을 **79자**로 열었다. `gh pr view --json title` 로 다른 걸
    보다가 우연히 걸렸다.
- **왜 조용한가**: 훅은 `git commit` 을 보고 `ai-attribution-guard` 는 커밋·PR
  명령을 보는데, **길이는 아무 가드의 관심사가 아니다.** 그리고 PR 제목은
  저장소 파일이 아니라 `make verify` 계열이 닿지 못한다.
- **제안**: `pr-create` 스킬 Step 5 가 `gh pr create` 를 부르기 **전에** 제목
  길이를 계산해 넘으면 멈춘다. 검증기가 아니라 스킬 본문이 자리인 이유는, 제목이
  존재하는 유일한 순간이 거기이기 때문이다. 훅으로 만들려면 `gh pr create` 의
  `--title` 을 파싱해야 하는데 인용 형태가 여러 가지라 취약하다.
- **왜 지금 안 고치나**: 하네스 변경이라 별도 PR 이다 (§6). 그리고 이 PR 은
  *빼는 작업* 이라 스킬 본문 변경을 섞지 않는다.
- **2026-08-08 — 3회차.** PR #9 를 **71자**로 열었다. 이 항목을 2회차로 적어둔
  바로 그 세션에서, 적은 지 몇 시간 만이다. **사람이 세는 규칙은 사람이 잊는다는
  것의 증거로 이보다 나은 것이 없다.**
- **고친 방법**: `pr-create` Step 5 가 `gh pr create` 직전에 `${#TITLE}` 를 세고
  넘으면 멈춘다. 검증기가 아니라 스킬 본문에 둔 이유는 **제목이 존재하는 유일한
  순간이 거기** 이기 때문이다 — PR 제목은 저장소 파일이 아니라 `make verify` 가
  닿지 못한다.
- **회차**: 3 (해결됨)

## 2026-08-08 — allow 티어에 검증 러너가 하나뿐이라 §4 를 지킬 수 없다

- **어디**: `plugins/harness-core/declarative/settings-fragment.json` — `permissions.allow`
- **무슨 일**: `CLAUDE.md` §4 는 *"run it, look at what came out"* 을 요구하고,
  새 R6 은 *"성공 기준을 실행 가능한 명령으로 적고 실행한다"* 를 요구한다. 그런데
  allow 42항목 중 **러너는 `Bash(make:*)` 하나뿐이다** — `python3`·`pytest`·`npm`·
  `sh`·`./script` 전부 없다. 즉 Makefile 이 없는 저장소에서는 **하네스가 명령한
  검증이 매번 승인 프롬프트를 띄우고, 헤드리스에서는 아예 불가능하다.**
- **어떻게 드러났나**: R6 벤치마크 1차 파일럿이 양팔 0/3 으로 나왔다. 스트림을
  뜯어보니 에이전트는 `sh check.sh` 를 **요청했고 거부됐다.** 즉 0/3 은 규칙에
  대한 증거가 아니라 권한에 대한 증거였다.
- **시험 대상 에이전트가 스스로 이 gap 을 보고했다** — `*harness gap*:
  .claude/settings.json:permissions.allow …` 한 줄로. 그리고 **이 기록에 적지도
  못했다**: `.claude/` 쓰기도 승인이 필요해 거부됐다. §5 의 "같은 턴에 기록하라"
  와 충돌하는 두 번째 지점이다.
- **경계가 이미 일관되지 않다**: `make` 는 허용돼 있는데 Makefile 은 임의 코드를
  실행한다. 즉 "테스트 러너는 임의 실행이라 위험하다" 는 선은 이미 넘어 있고,
  `pytest` 만 막혀 있다.
- **왜 2회차로 세나**: 이 기록의 *"`ask` 티어가 헤드리스에서 PR 플로우를 도달 불가로
  만든다"* 와 **같은 부류의 두 번째 발생** 이다 — 권한 설계가 하네스 자신이
  명령한 단계를 도달 불가로 만든다. 그쪽은 `ask`, 이쪽은 `allow` 의 누락이다.
- **제안**: allow 에 좁은 러너 항목을 더한다 — `Bash(pytest:*)` ·
  `Bash(python3 -m pytest:*)` · `Bash(npm test:*)` · `Bash(cargo test:*)` ·
  `Bash(go test:*)`. 임의 `python3`·`sh` 는 넣지 않는다 (그건 셸을 여는 것이다).
  **권한 변경이므로 승인 대상이고 별도 PR 이다.**
- **우회는 하지 않았다.** 벤치마크 픽스처를 `make check` 로 바꿔 지금 측정 가능하게
  했지만, 그건 계측기를 허용된 러너에 맞춘 것이지 gap 을 고친 것이 아니다.
- **회차**: 2 — 제안함, 승인 대기
## 2026-08-08 — 하네스가 "사용자에게 어떻게 보고하는가" 를 아예 다루지 않는다

- **어디**: `plugins/harness-core/declarative/CLAUDE.md` — §1~5 어디에도 없음
- **무슨 일**: 5원칙은 *코드를 어떻게 쓰는가* 만 다룬다. 사용자에게 나가는 보고의
  품질 — 자연스러운 한국어인가, 처음 보는 용어를 설명했는가, 구체적인가 — 은
  규약에도 가드에도 없다. 사용자가 외부 제안서를 들고 와 지적했다.
- **증거는 이 세션 안에 있다**: 내 보고가 `MDE 61%`, `짝비교`, `negative routing`,
  `2회차`, `§4b` 같은 표현을 설명 없이 쓴다. 이 저장소를 아는 독자에게는 효율적이고
  모르는 독자에게는 아무 정보가 아니다. **누가 독자인지 규약이 정해두지 않았으므로
  둘 중 어느 쪽으로도 틀렸다고 말할 근거가 없다** — 그것이 gap 이다.
- **제안된 구조**: `CLAUDE.md` 보고 규약 → Stop prompt hook 게이트 → 회귀 스위트.
  이 저장소의 3층 패턴(규약 → 가드 → 검증)과 같은 모양이다.
- **왜 지금 안 만드나**: 1회차이고, 게이트가 **턴마다 모델을 부른다.** 우리는 상시
  컨텍스트만 재고 **턴당 비용은 재는 장치가 없다.** 그리고 §4b 의 "전부 차단하는
  가드는 하루 만에 꺼진다" 가 정확히 이 종류에 걸린다 — 오탐률을 모르는 채로
  매 턴을 막는 가드를 배포할 수 없다.
- **먼저 할 것**: 이 세션의 실제 보고를 코퍼스로 리뷰어 프롬프트를 **오프라인으로**
  돌려 플래그율과 오탐을 센다. `bench-claims` 와 같은 모양이고 턴당 비용이 없다.
- **2026-08-08 — 쟀다.** 이 세션의 보고 40개(손으로 안 고름, 200자 이상 한글 포함)에
  리뷰어를 오프라인으로 돌렸다. 대조군 6/6 으로 계기는 유효했다.
  **플래그 31/40 (77%). 그중 18건이 독자가 소유한 라벨**(`§4b`·`R6`·`2회차`·`SOT`·
  `ADR-N`·`발생 기록`)**을 설명하라는 요구였다** — 자기가 쓴 문서의 절 번호를 자기에게
  설명하라는 것이라 이 독자에게는 오탐이다. 리뷰어는 **독자가 누구인지 모르므로**
  `§4b`(독자의 것)와 `artifact boundary`(에이전트가 지어낸 것)를 못 가른다.
  그리고 리뷰어 자신이 틀리기도 했다 — `'프로브에서'` 를 부자연스럽다며 *"아마
  '프로젝트에서' 를 의도했을 것"* 이라고 단정했다.
- **그래서 게이트는 배포하지 않는다.** 네 턴 중 세 턴이 막히고, 막는 이유의 절반
  이상이 독자 자신의 문서 인용이다. §4b 의 *"전부 차단하는 가드는 하루 만에 꺼진다"*.
  만들려면 **독자 컨텍스트를 입력으로 받아야 한다** — 측정이 밝힌 유일한 설계 결함이다.
- **남은 13건에는 진짜가 있었다**: 지시 대상 없는 숫자(`27/29` 가 무엇을 세는지 없음) ·
  소개 없이 등장한 이름(`체커`) · 앞 턴 참조(`Problem ②`). **그 셋만** `CLAUDE.md` §6
  으로 넣었다 (+277 tok, 여유 974 → 697).
- **회차**: 1 (부분 해결 — 규약은 넣고, 게이트는 근거를 갖고 보류)

## 2026-08-08 — Makefile recipe 의 셸은 어떤 검사도 안 거친다

- **어디**: `Makefile` — `verify-all` recipe
- **무슨 일**: recipe 에 `set -o pipefail` 을 썼다. macOS 에서는 `/bin/sh` 가 bash 라
  통과했고, **우분투 CI 에서 3초 만에 죽었다** — GNU make 는 recipe 를 `/bin/sh` 로
  돌리고 우분투의 `/bin/sh` 는 dash 라 `pipefail` 이 없다.
- **왜 안 잡혔나**: `make verify BASH=/bin/bash` 가 bash 3.2 바닥을 지키지만 그건
  **스크립트** 에만 적용된다. `make syntax` 도 `bash -n` 으로 `.sh` 만 본다.
  **Makefile recipe 본문은 어떤 문법 검사도 안 거친다.**
- **고친 방법**: 파이프를 없애고 임시 파일로 캡처했다. 이유를 recipe 주석에 적었다.
- **왜 규칙을 안 늘리나**: 1회차다. 그리고 답이 자명하지 않다 — recipe 를 dash 로
  `-n` 검사하려면 make 가 무엇을 셸에 넘기는지 재현해야 하고, `--dry-run` 출력을
  파싱하는 방식은 취약하다. `SHELL := /bin/bash` 를 Makefile 에 박는 쪽이 더 싸지만
  그건 모든 recipe 의 셸을 바꾸는 변경이라 별도 판단이 필요하다.
- **회차**: 1

## 2026-08-08 — README 가 고정을 권하는데 고정할 태그가 없다

- **어디**: `README.md` 시작하기 · `install.sh` 의 `--ref`
- **무슨 일**: 설치 안내를 다시 쓰면서 *"태그에 고정한다"* 예시로 `--ref v1.8.1` 을
  적었다. **이 저장소에는 태그가 0개다** (`git tag` · `git ls-remote --tags` 둘 다 0).
  플러그인 manifest 는 1.8.1 인데 그건 플러그인 버전이지 저장소 릴리스가 아니다.
  같은 회에 발견해 커밋 SHA 로 고쳤다.
- **왜 이게 gap 인가**: `--ref` 는 존재하고 동작하는데 **고정할 대상이 없다.**
  컨슈머에게 "main 이 움직인다" 를 알리면서 멈춰 세울 손잡이는 안 준 셈이다.
  오늘 하루에 PR 11개가 main 에 들어갔으므로 실제로 움직인다.
- **왜 지금 안 고치나**: 1회차이고, 태그 규약을 정하는 것은 별개 결정이다 —
  플러그인이 여섯이고 각자 버전이 따로 도는데 저장소 태그를 어느 것에 맞출지
  (또는 독립적으로 매길지) 정해야 한다. 지금 아무 태그나 붙이면 그 질문이
  답 없이 굳는다.
- **회차**: 1
- **해소 (2026-08-08)**: [ADR-0013](../docs/adr/0013-release-tags.md) 이 그 질문에
  답했다 — **저장소 태그는 어느 플러그인의 버전도 아니고 스냅샷의 버전이다.** bump
  규칙은 선언적 계약(설치기·`declarative/`)이 정하고, 플러그인 여섯은 각자 돈다.
  `v0.1.0` 에서 시작한다 (0.x 인 이유: 핵심 주장 다수가 아직 안 재졌다). README·
  `SECURITY.md` 의 *"릴리스 태그는 아직 없다"* 세 곳을 지웠다.

## 2026-08-08 — `headroom` 을 본문 대신 설치 상태로 판정했다

- **어디**: `docs/agent-layer.md` §3b 생태계 판정 표 — `headroom` · `ponytail` 행
- **무슨 일**: 그 행에 *"타 프로젝트 스코프의 비활성 설치. 이 저장소의 자산이
  아니다"* 라고 적었다. **그건 판정이 아니라 이 머신의 설치 상태 서술이다.**
  분야 조사에서 `ai-boost/awesome-harness-engineering` 의 Context Delivery 절에
  `headroom` 이 있고 설명이 *"Compresses tool outputs, logs, files before context
  entry (60–95% reduction)"* 였다. 실제로 우리 관심사 한복판이다.
- **왜 이게 반복인가**: [ADR-0011](../docs/adr/0011-ecosystem-survey.md) 이
  *"이름이 아니라 본문을 읽고 판정한다. 이름만 보고 내린 판정은 세 번 다 틀렸다"*
  를 결론으로 적어 두었다. **이번엔 이름도 아니고 설치 여부로 걸렀다** — 같은 병의
  네 번째다.
- **같이 놓친 것**: 같은 목록의 `codebase-memory-mcp` (저장소를 지속 지식그래프로
  인덱싱, **토큰 120배 감소** 주장) 는 우리 graph 축의 직접적 후보인데 §3b 에 없다.
- **왜 지금 안 고치나**: 판정하려면 본문을 읽어야 하고, 그건 별도 조사다. 다만
  **표에서 그 행을 지금 상태로 두면 안 된다** — "안 봤다" 를 "볼 필요 없다" 처럼
  적어두었기 때문이다. 다음 작업에서 §3b 를 고친다.
- **회차**: 1 (ADR-0011 의 일반화로는 4회차)
- **해소 (2026-08-08)**: 셋 다 본문을 읽고 판정했다. `headroom` 은 플러그인이 아니라
  로컬 프록시이고 유료 → `omniroute` 와 같은 이유로 **기각**. `ponytail` 은 MIT
  룰셋으로 우리 §2 와 같은 주장을 하는데 **저쪽은 쟀다** (LOC −54%, n=4, git diff
  채점) → **보류하되 측정 설계를 흡수**. `codebase-memory-mcp` 는 graph 축의
  구체적 후보로 표에 넣었고, 논문의 **0.83 vs 0.92** 가 "토큰 10× 절감이 품질 10%
  를 문다" 를 실측으로 보여준다. ADR-0011 의 그 행은 지우지 않고 **틀렸다고 표시**
  했다 — 뒤집힌 판정을 남기는 것이 `FINDINGS.md` R3 과 같은 규율이다.

## 2026-08-08 — 고쳤다고 보고한 편집이 안 들어갔고, 재현은 딴 브랜치에서 쟀다

- **어디**: PR #17 (`chore-generalize-language`) 의 세 findings 검증
- **무슨 일**: 두 가지가 겹쳤다.
  1. Makefile 을 python `s.replace(...)` 로 고치면서 `print("ok")` 를 **무조건**
     찍게 짰다. 앵커가 안 맞아 치환이 0회 일어났는데 출력은 `ok` 였다.
     직후 `grep -n HARNESS_TRIGGER_LANGS scripts/... Makefile` 이 스크립트 줄만
     내놨는데 — Makefile 이 없다는 그 사실이 곧 답이었는데 — 통과로 읽었다.
  2. 그 다음 재현을 `main` 에서 돌렸다. 셋 다 "재현 안 됨" 이 나왔고, 그건
     브랜치의 변경이 `main` 에 없기 때문이었다. 브랜치로 옮겨 다시 재니
     **셋 다 정확히 재현**됐다 (Error 127 · 조용한 pass · 6/11).
- **가드는 있었고 답을 인쇄했다**: compaction 직후 `session-brief` 훅이
  `- branch: main` 을 찍었다. 훅이 틀린 게 아니라 **출력을 안 읽었다.**
- **왜 이게 반복인가**: [ADR-0011](../docs/adr/0011-ecosystem-survey.md) 의
  *"본문을 읽고 판정한다"* 의 다섯 번째 변주다. 이번 대상은 생태계 저장소가 아니라
  **내 자신의 워킹 트리 상태**였다.
- **후보 규약** (2회차 대기): 편집 도구가 아닌 스크립트로 파일을 고쳤으면
  치환 횟수를 확인하고, 재현·측정 전에 `git branch --show-current` 를 찍는다.
  전자는 이번에 `if old not in s: sys.exit(...)` 로 처리했다.
- **회차**: 1 (ADR-0011 의 일반화로는 5회차)

## 2026-08-08 — 배포되는 `bin/` 실행파일에는 산출물 규정이 없다

- **어디**: `CLAUDE.md` §2 (새 훅) · §2b (새 스킬) · `.claude/agents/harness-reviewer.md`
- **무슨 일**: `harness-log` 를 `plugins/harness-core/bin/` 에 넣으면서 무엇을 같이
  내야 하는지 정한 규정이 **없다는 것을 발견했다.** 훅은 6산출물, 스킬은 4산출물이
  명문화돼 있는데 실행파일은 어느 쪽도 아니다. 훅 규정을 유추해서 (스크립트 ·
  검증기 · 문서 · SOT · version bump · README) 냈고 감사는 통과했지만,
  **유추가 규정을 대신하면 다음 사람은 다르게 유추한다.**
- **감사가 실제로 잡은 것**: `Makefile` 의 `syntax` 타깃이 `bin/harnessctl` 을
  하드코딩하고 있어 새 실행파일이 `bash -n` 에서 조용히 빠졌다. `docs/agent-layer.md`
  §4 가 *"목록을 하드코딩하는 대신 glob"* 이라고 적어둔 바로 그 실패다. glob 으로
  고쳤다.
- **후보 규약** (2회차 대기): `CLAUDE.md` 에 §2c — 실행파일 산출물 묶음.
  훅과 다른 항목은 둘이다: `hooks.json` 등록 대신 **`install.sh` 의 shim** 이
  필요하고 (플러그인 `bin/` 은 Bash 도구 PATH 에만 오르지 사용자 터미널에는 안
  오른다), 문서는 `docs/hooks/` 가 아니라 `docs/<name>.md` 다.
- **회차**: 1

## 2026-08-08 — `install.sh` 에는 동작 검증기가 없다

- **어디**: `install.sh` 174–195 (shell shim) · `scripts/verify-install.sh`
- **무슨 일**: shim 블록은 `harnessctl` 이라는 이름을 **하드코딩** 하고 있었다.
  그래서 두 번째 실행파일을 배포하는 순간, 문서가 알려주는 터미널 명령이 존재하지
  않는 상태가 된다. 아무 검사도 이것을 잡지 않았다 — 감사 에이전트가 잡았다.
- **왜 안 잡혔나**: `verify-install.sh` 는 이름과 달리 **`harnessctl` 만** 시험한다
  (92 assertion 전부). `install.sh` 는 `make syntax` 의 `bash -n` 만 받는다.
  파싱은 하드코딩된 이름을 잡을 수 없다.
- **이번에 한 것**: 이름 대신 `bin/` 을 glob 하도록 고치고, 가짜 플러그인 캐시
  (버전 둘 + 비실행 파일 하나) 를 만들어 손으로 확인했다 — shim 둘 생성, `README`
  제외, `sort -V` 가 1.10.0 을 1.9.0 보다 뒤로 놓음. **손 확인은 회귀를 막지
  못한다.**
- **왜 지금 규정을 안 만드나**: `install.sh` 에 동작 검증기를 붙이려면 marketplace
  등록과 플러그인 설치를 오프라인에서 흉내내야 하고, 그건 세션 로그 렌더러 PR 보다
  큰 작업이다. 범위를 섞지 않는다 ([`CLAUDE.md`](../CLAUDE.md) §6).
- **회차**: 1
## 2026-08-08 — `install.sh` 8행이 주석 표시를 잃고 다섯 릴리스를 살아남았다

- **어디**: `install.sh:8` — usage 헤더의 `./install.sh --profile research,slides` 줄
- **무슨 일**: `#` 이 없다. 주석 블록 한가운데 있어 주석처럼 보이지만 **매 실행마다
  가장 먼저 실행되는 명령**이다. 클론 디렉터리에서 `bash install.sh` 를 돌리면
  자기를 무한히 다시 부른다 (깊이 카운터를 붙여 재현했다: depth=1,2,3,4…).
  `curl -o` 로 받은 644 복사본에서는 `Permission denied` 한 줄을 뱉고 계속한다.
- **언제부터**: `c69f8cb` "Give the README a front page…" — 저장소 최근 커밋 5개 중
  하나이고, 그 뒤 PR 이 다섯 번 넘게 main 에 들어가는 동안 아무도 못 봤다.
- **왜 아무 검사도 못 잡았나**: `make syntax` 의 `bash -n` 만 install.sh 를 본다.
  **파싱은 주석과 명령을 구분하지 못한다** — 그 줄은 완벽하게 유효한 bash 다.
  `verify-install.sh` 는 이름과 달리 `harnessctl` 만 시험했다.
- **이것이 바로 앞 항목의 2회차다.** 같은 날 *"`install.sh` 에는 동작 검증기가
  없다"* 를 1회차로 적었고 (shim 이 이름을 하드코딩한 건), 그 직후 이 버그를 찾았다.
  1회차에 *"동작 검증기를 붙이는 건 이 PR 보다 큰 작업"* 이라고 적었는데, **2회차가
  그 판단을 뒤집었다** — 큰 작업이 아니라 7 assertion 이었다.
- **고친 것**: `#` 복원 + `verify-install.sh` 에 install.sh 절 (92 → 99).
  핵심은 **정적 검사**다: `set -uo pipefail` 위쪽에 빈 줄도 `#` 도 아닌 줄이 있으면
  실패. 한 줄이 아니라 부류를 막는다.
- **검증기 자신에 대한 교훈 하나**: 첫 판본은 install.sh 를 *자기 복사본이 있는
  디렉터리에서* 돌렸다. 컨슈머가 하는 그대로지만, 버그가 있는 상태에서는 **검증기가
  검사 대상 기계를 fork bomb 한다.** 실제로 변이 테스트 중에 그렇게 됐고 죽여야
  했다. 지금은 빈 디렉터리에서 절대경로로 부른다 — 같은 `./install.sh` 가 아무것도
  못 찾으므로 시도는 stderr 에 남고 아무것도 생기지 않는다.
- **회차**: 2 (앞 항목의 반복)


## 2026-08-11 — 활성 gh 계정과 커밋 author 는 서로 독립인데 아무것도 안 본다

- **어디**: `plugins/harness-core/hooks/gh-account-guard.sh` (일부러 범위 밖) ·
  `docs/hooks/gh-account-guard.md` Limits 절
- **무슨 일**: `gh` 가 git 의 credential helper 로 걸려 있어서 `git push` 는
  **활성 계정**의 토큰을 쓴다. 그런데 `git config user.email` 은 **완전히 별개의**
  전역 값이다. 이 기계가 지금 그 상태다 — 활성 계정 `chpark-ML`, `user.email`
  `bch0322@gmail.com`. 즉 push 자체는 옳은 계정으로 나가면서 커밋 author 는
  틀릴 수 있고, 그 반대도 된다. 회사 repo 에 개인 메일이, 공개 repo 에 회사
  메일이 남는 경로가 열려 있다.
- **왜 이번에 안 고쳤나**: 요청자가 지목한 사고 두 개가 *잘못된 정체성으로 PR/
  커밋이 올라감* 과 *권한 없어 거부됨* 이었고, 둘 다 토큰 쪽이다. email 을 같이
  판정하면 설정 파일이 `<login> <email>` 짝이 되고 검증 케이스가 대략 배로 늘어
  난다. 범위를 섞지 않았다 (`CLAUDE.md` §2).
- **2회차가 되는 조건**: 실제로 author 가 틀린 커밋이 한 번 더 나오면. 그때는
  `gh-account.txt` 를 짝으로 확장하는 것이 자연스러운 모양이다.
- **회차**: 1

## 2026-08-11 — §7 의 "2회 발생" 게이트를 의식적으로 넘겼다

- **어디**: `CLAUDE.md` §7 · `docs/agent-layer.md` §7 백로그
- **무슨 일**: `gh-account-guard` 는 **발생 횟수가 확립되지 않은 상태에서** 만들어
  졌다. 검토 초반에 §7 을 근거로 세 가지 선택지(2회 이상이면 훅 / 1회면 기록+
  백로그 / 0회면 백로그 한 행)를 제시했고, 요청자가 *"횟수 무관, 가드를 바로
  만들어달라"* 를 택했다.
- **왜 적어두나**: 이것은 **판단이지 선례가 아니다.** 기록해두지 않으면 다음
  사람이 `gh-account-guard` 의 존재를 보고 "훅은 1회 발생에도 만든다" 로 읽는다.
  §7 이 지키려는 것은 just-in-case 방어를 막는 것이고, 그 규정은 여전히 유효하다.
  이번 건은 요청자가 규정의 비용을 알고 지불한 예외다.
- **완화한 것**: 범위를 사고 두 개에만 맞췄다. `gh issue create` 와
  `gh release create` 도 정체성을 걸고 쓰지만 발생한 적 없어 catch 목록에서 뺐고,
  `user.email` 도 같은 이유로 뺐다 (위 항목).
- **회차**: 1

## 2026-08-11 — §5 는 인벤토리가 한 곳이라 말하는데 훅 개수는 열 곳에 있다

- **어디**: `CLAUDE.md` §5 · `docs/adr/0004-single-source-of-truth.md` ·
  `README.md:121,137-145,160` · `README.ko.md:46,259,280,282-290,343` ·
  `docs/engineering-axes.md:42` · `docs/agent-layer.md:67,384,387`
- **무슨 일**: 훅 하나를 추가하면서 `docs/agent-layer.md` 만 고치면 된다고 §5 를
  읽었다. **틀렸다.** `harness-reviewer` 감사가 훅 개수와 가드 표가 **최소 10곳**
  에 중복돼 있음을 잡았다. 배지 숫자는 `verify-check-total.sh` 가 기계적으로
  지키지만, *훅 개수* 와 *가드 표* 는 아무것도 지키지 않는다.
- **아이러니**: `ADR-0004:29` 이 *"§3 의 인벤토리 숫자는 여전히 손으로 관리되고
  드리프트할 수 있다"* 고 자기 입으로 적어놓고, 그 문장 안의 "6 hooks" 가 바로
  드리프트했다.
- **증거로 딸려 나온 것**: `92 assertions` 가 세 곳에 남아 있었다
  (`engineering-axes.md:59`, `agent-layer.md:189,315`). 92 → 99 는 **내 변경
  이전에** 일어난 일이고 아무도 못 봤다. 그 중 둘이 SOT 안이라 §4 의 숫자와
  자기모순 상태로 있었다. 이번에 104 로 함께 썼다.
- **후보 규약** (2회차 대기): `verify-check-total.sh` 가 배지에 하는 것을 훅
  개수에도 한다 — `hooks.json` 의 항목 수와 blocking/informational 분류를
  세어서, 그 숫자를 적은 모든 문서와 대조. 파일 목록은 glob 이어야 한다
  (`agent-layer.md` §4 가 하드코딩 대신 glob 이라 적어둔 그 이유).
- **왜 지금 안 만드나**: 이번 PR 은 훅 추가다. 구조 변경과 내용 추가는 다른 PR
  이다 (`CLAUDE.md` §6). 그리고 이것이 1회차다.
- **회차**: 1

## 2026-08-11 — 문서가 시키는 명령이 실제로 그 일을 하는지는 아무도 안 본다

- **어디**: `docs/hooks/gh-account-guard.md` Configuration 절 ·
  `plugins/harness-core/declarative/templates/gh-account.txt` · 세션에서 내가
  사용자에게 준 가이드
- **무슨 일**: `gh-account.txt` 는 **첫 줄만** 읽는데, 셋 다 `echo 'login' >>` 를
  안내했다. 오타를 내고 다시 `>>` 로 "고치면" 가드는 여전히 첫 줄을 쓰면서 방금
  바꿨다고 생각한 계정으로 계속 막는다. 빠져나올 길이 안 보이는 상태다.
- **왜 아무 검사도 못 잡았나**: `verify-doc-refs` 는 **참조된 파일이 존재하는지**
  만 본다. `echo … >> …` 는 문법적으로 멀쩡하고 가리키는 파일도 실재한다.
  훅 동작은 `verify-gh-account-guard` 가 26 assertion 으로 지켰지만, **문서가
  시키는 절차** 는 어느 검증기의 대상도 아니었다. 사용자가 "이거 의도대로
  동작하나?" 라고 물어서 재보고 나서야 나왔다.
- **이번에 한 것**: 문서·템플릿에 "첫 줄만 읽으므로 append 하지 말고 편집하라" 를
  명시. 그리고 애초에 파일이 거의 필요 없도록 remote owner 추론을 넣었다 — 가장
  안전한 절차는 **없는 절차**다.
- **후보 규약** (2회차 대기): 문서의 fenced bash 블록 중 부작용 없는 것을 실제로
  실행해보는 검증기. 범위가 넓고 오탐이 쉬우니, 우선은 "훅이 읽는 설정 파일을
  만드는 명령" 처럼 좁은 부류부터.
- **회차**: 1

## 2026-08-11 — 문서가 시키는 명령이 그 일을 하는지 아무도 안 본다 (2회차)

- **어디**: `README.md` · `README.ko.md` 시작하기 절
- **무슨 일**: "rule 을 저장소에 넣으려면" 하고 `harnessctl init --scope project` 를
  적었다. 그 명령은 **catch-all `workflow.md` 하나만** 깐다. `harnessctl` 은 모듈을
  `--with` 아니면 대상 매니페스트에서 가져오는데, 처음 설정하는 저장소에는 둘 다
  없기 때문이다. 영어판 기본값은 `dev`·`research` rule 둘을, 한국어판
  `--profile dev,python` 예시는 `dev` rule 을 조용히 빠뜨렸다. Codex 리뷰가 P1 로
  잡았다.
- **1회차와 같은 부류인 이유**: 앞의 `echo … >>` 와 똑같이, 명령은 문법도 맞고
  실행도 되는데 **문서가 약속한 결과를 내지 않는다.** 어느 검사도 이 종류를 보지
  않는다.
- **더 나쁜 사실 하나**: 이번엔 내가 **그 명령을 실제로 돌려보고 통과라고 적었다.**
  "rule 이 깔리나"를 봤고 `workflow.md` 가 나오길래 넘어갔다. 물었어야 할 것은
  "**고른 프로파일의 rule 들이** 깔리나" 였다. 검증을 했다는 사실이 올바른 속성을
  검증했다는 뜻은 아니다.
- **같이 드러난 것**: `install.sh` 에 `--dry-run` 이 없는데 "어느 명령이든" 붙이면
  된다고 적었다 (`unknown argument` 로 죽는다). 그리고 `~/.local/bin` 이 PATH 에
  없으면 바로 뒤의 `harnessctl` 명령이 `command not found` 인데, 내 기계에는 이미
  PATH 에 있어서 확인이 통과했다.
- **제안 (2회차 도달)**: `docs/` 와 두 README 의 fenced bash 블록 중 **부작용 없는
  것**을 실제로 실행하는 검증기. 최소 범위로 시작한다 — 하네스 자신의 명령
  (`harnessctl …`, `install.sh …`) 이 등장하는 블록에 대해 (1) 플래그가 실재하는지
  `--help` 와 대조, (2) `--dry-run` 이 있는 명령은 돌려서 약속한 산출물 목록과 대조.
  플래그 대조만으로도 이번 셋 중 둘이 잡혔다.
- **회차**: 2 (2026-08-11 "문서가 시키는 명령…" 의 반복)

## 2026-08-11 — 훅 개수가 또 낡아 있었다 (2회차)

- **어디**: `README.ko.md` 프로파일 표 3곳 (`가드 6`) · `README.md` 1곳 (`6 guards`)
- **무슨 일**: 어제 감사가 열 곳을 찾아 쓸었는데, **같은 두 파일에 네 곳이 더
  남아 있었다.** 감사가 훑고 지나간 파일에서, 하루 뒤 다른 작업 중에 눈에 띄었다.
- **왜 2회차인가**: 앞 항목(§5 는 인벤토리가 한 곳이라는데 훅 개수는 열 곳에 있다)
  과 같은 원인이고, **그 항목이 제안한 대책이 없었기 때문에 재발했다.** 손으로
  쓸면 손이 놓친 만큼 남는다는 것이 이번 증거다.
- **제안 (2회차 도달)**: `verify-check-total.sh` 가 배지에 하는 것을 훅 개수에도
  한다. `hooks.json` 에서 항목 수와 blocking/informational 분류를 세고, 그 숫자를
  적은 모든 문서와 대조. 파일 목록은 하드코딩이 아니라 glob 이어야 한다
  (`docs/agent-layer.md` §4 가 같은 이유로 그렇게 적어둔 항목이 있다).
- **회차**: 2 (2026-08-11 "§5 는 인벤토리가…" 의 반복)

## 2026-08-11 — context ceiling 이 CI 에서 한 번도 강제된 적이 없다

- **어디**: `scripts/context-budget.sh:77` · `.github/workflows/verify.yml` ·
  `README.md` · `README.ko.md` · `docs/agent-layer.md` · `Makefile`
- **무슨 일**: `context-budget.sh` 는 Claude CLI 가 없으면 플러그인 비용을 전부
  0 으로 세고 그대로 통과한다. CI 세 잡 중 ceiling 을 돌리는 둘(`verify-all`,
  `verify`)에는 **CLI 가 없고**, CLI 를 까는 잡은 `verify-plugins` 만 돌렸다.
  즉 *"전 프로파일 9,000 토큰 이하"* 는 **한 번도 검사된 적이 없다.**
- **결과**: 공표 숫자가 네 문서에서 **세 값**으로 갈라졌다 — README.md 8,305 /
  README.ko.md·SOT·Makefile 8,026 / 실측 **7,934**. 어느 것도 맞지 않았다.
- **하필**: `CONTRIBUTING.md:127` 이 *"Numbers in documents are generated, not
  typed"* 라고 적어둔 문단이, **바로 그 수치가 3.6배 틀렸던 사고**를 설명하는
  문단이다. 재발 방지를 적어둔 자리 옆에서 같은 부류가 재발했다.
- **`CLAUDE.md` §4 의 거울상**: *"검증기가 도는 환경 중 하나에서만 실행되는 줄은
  검증되지 않은 줄"* — `verify-check-total` 은 CLI 있는 기계에서 써서 CI(없음)
  에서 깨졌고, 이번엔 정반대 방향이다. **같은 문장이 양쪽을 다 설명한다.**
- **또 하나**: CLI 만 있고 플러그인이 설치돼 있지 않아도 비용은 0 이다. 그래서
  "CLI 잡에 `make context-budget` 을 추가" 라는 처방은 **그대로는 안 통한다** —
  `claude plugin details` 는 설치된 플러그인에만 답한다. 리뷰가 제안한 수정을
  그대로 냈으면 아무것도 안 고치는 수정이 됐다.
- **고친 것**: `--require-plugins` (부분 측정·낡은 설치본·문서 불일치면 실패),
  버전 표시(설치본이 트리보다 낡으면 그렇다고 출력), 공표 숫자 4곳 대조
  (`verify-check-total` 이 배지에 하는 것과 같은 일), CI 의 CLI 잡에서
  marketplace 등록 + 플러그인 설치 후 `context-budget-strict`.
- **회차**: 1

## 2026-08-11 — 상시 컨텍스트 수치는 저장소만의 속성이 아니다

- **어디**: `scripts/context-budget.sh` · 공표 수치 4곳 · (관련) `CLAUDE.md` §8,
  ADR-0011 의 한국어 트리거 결정
- **무슨 일**: 같은 커밋을, **같은 Claude Code 2.1.227** 로 재는데 값이 다르다.
  로컬(macOS) 7,934 / CI(ubuntu) 7,085. 플러그인별로 보면 하네스 것들이 거의
  정확히 2배다 — core 329/159, dev 240/113, research 481/245, slides 446/234.
  반면 `superpowers` 는 688/584 로 1.18배뿐이다.
- **차이의 정체**: 우리 스킬 설명문에는 **한국어 트리거 절**이 들어 있고
  superpowers 는 처음부터 끝까지 영어다. 추정기가 한국어를 환경에 따라 다르게
  센다.
- **왜 중요한가**: "문서 숫자 = 측정값" 을 강제하면 **우리 결함이 아니라 남의
  릴리스나 다른 러너로 CI 가 빨개진다.** §4 가 말하는 늑대 부르기이고, 그렇게
  꺼진 검사는 없는 검사다. 그래서 게이트는 **ceiling(상한)** 으로 두고 숫자
  대조는 경고로 낮췄다. 상한은 등식이 아니라 경계라 어느 회계에서도 성립한다.
- **§8 에 남는 질문**: 한국어 트리거 절은 *효과* 가 유의하지 않아 유지된 결정인데
  (pass^3 0.83 vs 0.50, Fisher p=0.545), 이번에 **비용 쪽이 환경을 탄다**는 사실이
  새로 나왔다. 우리 플러그인 상시 비용의 상당 부분이 그 절이라는 뜻이다. 비용을
  다시 재는 것이 그 결정의 2회차 재검토 근거가 될 수 있다. 지금은 기록만 한다.
- **회차**: 1

## 2026-08-11 — shim 은 CLAUDE_CONFIG_DIR 을 무시했고, orphaned 버전도 안 걸렀다

- **어디**: `install.sh` shim 생성 블록과 shim 본문 · `scripts/verify-install.sh`
- **무슨 일 (1)**: `install.sh:108` 은 `CFG="${CLAUDE_CONFIG_DIR:-$HOME/.claude}"`
  로 harnessctl 을 찾는데, **shim 쪽 두 곳은 `$HOME/.claude` 를 하드코딩** 했다.
  기본이 아닌 config 디렉터리를 쓰면 glob 이 아무것도 못 맞춰 루프가 0회 돌고,
  경고 한 줄만 남긴 채 **설치는 성공한 것처럼 끝난다.** Codex 저장소 리뷰가 P2 로
  지적했고 재현됐다.
- **무슨 일 (2) — 케이스를 쓰다가 나온 것**: shim 은 `sort -V | tail -1` 로 최신
  버전을 고르는데 **`.orphaned_at` 을 안 걸렀다.** `claude plugin update` 는 옛
  버전을 지우지 않고 마커를 달고 `bin/` 을 비운다. 이 기계 캐시에 실제로 그런
  디렉터리가 셋 있다 (`figma/2.2.81`, `pyright-lsp/1.0.0`, …, 전부 bin 0개).
  orphaned 가 라이브보다 높은 버전이면 shim 이 **빈 디렉터리를 골라 exec 에
  실패한다.** 3단계는 이 마커를 처음부터 걸렀다 — **같은 파일 안의 두 해석기가
  서로 다른 규칙을 쓰고 있었다.**
- **왜 아무 검사도 못 잡았나**: `verify-install.sh` 는 이름과 달리 install.sh 에
  대해 `--help` 와 인자 처리만 봤다. **컨슈머 터미널이 실제로 의존하는 블록에
  케이스가 하나도 없었다.** 이것은 이 기록의 *"install.sh 에는 동작 검증기가 없다"*
  (2026-08-08, 2회차) 가 예고한 바로 그 자리다.
- **고친 것**: 두 해석기를 `newest_bindir()` 하나로 합치고 (마커 필터 포함), shim
  본문은 실행 시점에 `${CLAUDE_CONFIG_DIR:-$HOME/.claude}` 를 해석한다 — 설치
  때 값을 굽지 않으므로 나중에 config 디렉터리를 옮겨도 안 깨진다.
  `verify-install.sh` 에 8 케이스 (99 → 112). 변이 셋으로 물리는 것을 확인했다:
  shim 하드코딩 3개 실패, 생성 하드코딩 1개, orphaned 필터 제거 6개.
- **교훈**: 두 번째 버그는 **첫 버그의 케이스를 쓰다가** 나왔다. 픽스처를 진짜
  캐시 모양에 맞추려고 `.orphaned_at` 을 넣었더니 shim 이 깨졌다. 케이스를 실제
  구조로 쓰는 것이 그 자체로 탐색이다.
- **회차**: 1 (앞의 "install.sh 동작 검증기" 항목의 구체적 실현)

## 2026-08-11 — [닫힘] 인벤토리 수치를 대조하는 검사를 만들었다

- **닫는 항목**: 2026-08-11 *"§5 는 인벤토리가 한 곳이라 말하는데 훅 개수는 열
  곳에 있다"* (1회차) → *"훅 개수가 또 낡아 있었다"* (2회차). §5 의 문턱을
  넘었으므로 제안이 아니라 구현으로 닫는다.
- **만든 것**: `scripts/verify-inventory.sh` (23 케이스), `make verify` 에 편입.
  수치는 **그것을 정의하는 산출물에서** 뽑는다 — 훅은 `hooks.json` 등록 수
  (PreToolUse 5 / 그 외 2), permission 은 `settings-fragment.json` 의 배열 길이,
  assertion 은 `verify-install.sh` 를 **실제로 돌려서** 나온 수. 다른 문서를
  진실로 삼지 않는다.
- **덤으로 잡히는 것 둘**: 등록됐는데 없는 스크립트, 있는데 등록 안 된 스크립트.
  둘 다 개수는 맞는데 하네스는 깨진 상태다.
- **의도적 한계**: 문구를 glob 하지 않고 **열거**한다. 새 문장으로 개수를 적으면
  누가 패턴을 추가할 때까지 안 잡힌다. 대안(README 의 모든 `7` 을 훑기)은
  오탐 기계이고, 늑대를 부르는 검사는 꺼진다 (ADR-0003). 실제로 일어나는 사고는
  *"있던 문장이 낡는 것"* 이고 그건 잡는다.
- **재현으로 확인**: 오늘 실제로 샌 두 사고를 되돌려 넣었더니 둘 다 잡혔다 —
  `가드 훅 6개` (README.ko 만 낡음), `104 assertions` (engineering-axes 만 낡음).
  문장을 리워딩해 패턴이 안 맞게 만드는 경우도 통과가 아니라 실패로 나온다.
- **왜 생성기가 아닌가**: 리뷰는 `<!-- BEGIN GENERATED -->` 블록과 생성 스크립트를
  제안했다. 이 저장소는 이미 `verify-check-total.sh` 로 **생성이 아니라 대조** 를
  한다. 대조가 더 작은 기계장치이고, 산문을 사람 손에 남기며, 같은 자리에서
  크게 실패한다. 있는 관행을 넓히는 쪽을 택했다.
- **회차**: — (2회차 항목을 닫는 구현)

## 2026-08-11 — 제거 안내가 넷 중 하나만 말했다

- **어디**: `plugins/harness-core/bin/harnessctl` uninstall 종료 안내 ·
  `README.md` / `README.ko.md` 되돌리기 절
- **무슨 일**: 기본 설치는 플러그인 넷(+의존성 superpowers)을 까는데, uninstall 이
  찍는 안내와 README 예시는 **하나만** 지운다. 그대로 따르면 나머지가 남는다.
  Codex 저장소 리뷰 지적.
- **왜 하드코딩이었나**: harnessctl 은 플러그인을 설치하지도 제거하지도 않으므로
  그 목록을 "모른다"고 여겼다. 사실은 캐시를 읽으면 알 수 있었다 — 다른
  의존성(`claude`)을 새로 들이지 않고, 프로파일이 추가돼도 안 낡는다.
- **고치다가 나온 함정**: 처음엔 `.in_use` 마커로 라이브 버전을 판정했는데,
  **현재 버전에 그 마커가 없는 경우를 관측했다** (update 직후로 추정). 그걸
  요구하면 잘 설치돼 동작 중인 플러그인이 "없음" 으로 보고된다. 신뢰할 수 있는
  신호는 `.orphaned_at` **부재** 이고, 이는 install.sh 의 shim 해석 규칙과 같다.
- **덤**: doctor 가 shim 목록과 그 제거 명령도 출력한다. shim 은 install.sh 가
  쓰고 harnessctl 이 안 지우므로, 완전 제거에는 원래 포함돼야 했다.
- **회차**: 1

## 2026-08-11 — [닫힘] #24 를 전제 충돌로 닫고, 쓸모 있는 절반만 doctor 로

- **어디**: PR #24 (`scripts/clean_user_harness.sh`, 277줄) · `harnessctl doctor`
- **무슨 일**: 8월 8일에 연 PR 이 사흘간 열려 있었고, 그 사이 오늘 문서가 **반대
  입장**을 채택했다. #24 는 *"하네스는 project 스코프의 것이고 user 스코프는
  청소 대상"* 이라고 열었는데, 오늘 머지한 README 는 *"가드와 스킬은 내 것이라
  기계 전체에 있어야 한다 — 설치를 잊은 저장소 하나가 곧 구멍"* 이라고 적는다.
  같은 질문에 반대 답이 저장소 안에 둘 생길 뻔했다.
- **왜 이런 일이**: PR 이 열려 있는 동안 그 PR 이 전제하는 설계가 다른 PR 에서
  바뀌었고, **아무것도 그 충돌을 알려주지 않았다.** 머지 가능 여부(`MERGEABLE`)는
  텍스트 충돌만 본다. 사흘 묵은 PR 을 오늘 상황 보고를 만들다 우연히 발견했다.
- **살린 것**: #24 가 찾은 *해석 안 되는 `enabledPlugins` 항목* 은 스코프 논쟁과
  무관한 설정 무결성 문제이고 아무도 안 보고 있었다. `doctor` 로 옮겼다 —
  별도 스크립트였으면 케이스가 없었을 것이고, 오늘 고친 `bench-tier` 고아 부류를
  하나 더 만들 뻔했다.
- **설계 판단 하나**: 해석 안 되는 항목은 **fail**(켜졌다고 적혀 있는데 못 뜬다),
  아무도 안 쓰는 marketplace 는 **note**(무해하다). 무해한 상태에 경보를 울리는
  doctor 는 아무도 안 읽는다. 변이로 둘 다 고정했다.
- **후보 규약** (1회차): 오래 열린 PR 은 머지 전에 *전제가 아직 유효한가* 를
  본다. 지금은 §6 이 "PR 열기 전 이 기록을 읽어라" 만 말한다.
- **회차**: 1

## 2026-08-12 — [닫힘] 문서가 시키는 명령을 검사한다

- **닫는 항목**: 2026-08-11 *"문서가 시키는 명령이 실제로 그 일을 하는지 아무도
  안 본다"* (1회차 → 2회차). §5 문턱을 넘었으므로 구현으로 닫는다.
- **만든 것**: `scripts/verify-doc-commands.sh` (실문서 45 + selftest 12),
  `make verify` 에 편입. 문서의 fenced 블록에서 하네스 자신의 명령을 뽑아
  서브커맨드와 롱 플래그를 **파서와** 대조한다 — `--help` 가 아니라 파서다.
  usage 산문도 따로 낡을 수 있고, 그게 지금 잡으려는 부류다.
- **범위**: 기록에 적어둔 제안의 좁은 절반만. 블록을 실제로 실행하는 것은 블록마다
  샌드박스가 필요해 비싸고 불안정하다. 플래그 대조만으로 기록된 사고 셋 중 둘이
  잡힌다.
- **오탐이 진짜 비용이었다**: 처음 돌리자 6건이 실패했고 **전부 내 추출기의
  오탐**이었다. 원인 넷 — (1) `harnessctl init` 을 서브커맨드로 못 봄(`init` 은
  fall-through 라 `[ "$CMD" = init ]` 비교가 없다. usage 블록에서 뽑도록 고침),
  (2) `remove with:  rm ~/.local/bin/harnessctl` 이 서브커맨드 `with:` 인 호출로
  읽힘, (3) 출력 예시 안의 경로가 명령으로 읽힘, (4) `\` 연속 줄이 명령으로
  읽힘. 셋 다 **세그먼트 첫 토큰 앵커링**으로 해결 — `gh-account-guard` 게이트가
  같은 이유로 배운 것이다.
- **내가 또 밟은 함정**: 세그먼트 분해를 `printf | tr | while` 로 썼다. 파이프
  뒤의 `while` 은 서브셸이라 카운터가 통째로 사라진다 — **그 훅 주석에 내가 직접
  적어둔 함정이다.** heredoc 으로 고쳤다.
- **약한 단정 하나**: "출력 예시 속 경로는 명령이 아니다" 케이스가 특정 실패
  문자열을 기다리고 있어, 앵커를 깨는 변이에서 **엉뚱한 이유로 통과**했다.
  "그 파일에서 실패가 하나도 안 나온다" 로 바꾸니 변이가 물린다. 케이스가 무엇을
  기다리는지가 케이스의 강도다.
- **회차**: — (2회차 항목을 닫는 구현)

## 2026-08-12 — 훅이 독립 코퍼스 없이 배포됐다

- **어디**: `evals/incidents.sh` · `CLAUDE.md` §2 (새 훅 산출물 6개)
- **무슨 일**: `gh-account-guard` 는 검증기(45 assertion)를 갖췄지만 **독립
  코퍼스에는 케이스가 0개** 인 채로 배포됐다. §2 의 훅 번들 6개 항목에
  `evals/incidents.sh` 가 없기 때문이다 — 반면 §2b 의 스킬 번들은
  `evals/trigger/<name>.json` 을 명시적으로 요구한다. 훅과 스킬이 *독립 측정*
  에서 비대칭이다.
- **왜 중요한가**: 그 사이 공표 수치 `27/29` 는 **차단 훅 5개 중 4개** 를 잰
  값이면서 "가드 층" 을 잰 것처럼 읽혔다. 검증기는 가드 자신의 regex 에서 나온
  케이스로 채점하고, 코퍼스는 사고 표에서 독립적으로 쓴다 — 후자가 빠지면
  "우리가 막으려던 것" 이 아니라 "우리가 짠 것" 만 재게 된다.
- **이번에 한 것**: `ghaccount` 카테고리 12 케이스(차단 6 · 통과 6)를 §2 사고 표
  행에서 독립적으로 작성. 새 측정 행 — **33/35, 오탐 2/30**. 옛 수치를 고친 것이
  아니라 새로 잰 것이다 (`CLAUDE.md` §8).
- **후보 규약** (1회차): §2 에 7번째 산출물 — 차단 훅은 `evals/incidents.sh` 에
  케이스를 낸다. 승격하지 않는다; 1회차다.
- **2026-08-17 해결됨**: 같은 부류가 넷이 되어 §2 · §2b · §2c 를 한 번에 고쳤다.
  이 항목이 제안한 7번째 산출물이 §2 에 그대로 들어갔다.

## 2026-08-12 — 검증기가 자기 출력을 인코딩하다 죽었다 (3회차)

- **어디**: `scripts/verify-frontmatter.sh:124,126` · `scripts/verify-doc-refs.sh:273`
  · `scripts/verify-benches.sh:88`
- **무슨 일**: 한국어 Windows(cp949 콘솔)에서 `make verify` 가 `frontmatter` 에서
  멈춘다. 증상이 고약하다 — **11 / 11 전부 통과하고 나서** 요약 줄을 찍다가
  `UnicodeEncodeError: 'cp949' codec can't encode character '—'` 로 죽고
  rc=1 을 낸다. **초록불 실행이 실패로 보고된다.** 파이썬의 stdout 은 로케일
  인코딩에 `errors='strict'` 인데, 이 저장소의 산문은 em-dash 를 쓴다.
- **왜 아무 검사도 못 잡았나**: `CLAUDE.md` §4 의 *"검증기가 도는 환경 중
  하나에서만 실행되는 줄은 검증되지 않은 줄"* 의 **3회차**다. 앞의 둘
  (`verify-check-total` 의 CLI-없음 분기, `verify-all` 의 `pipefail`) 은 전부
  **CI-대-로컬** 축이었다. 이번은 **로케일** 축이고, 그래서 아무도 안 봤다 —
  그 줄은 개발 환경에서 매번 실행됐다. 실행되지 않은 게 아니라, **실행되는
  인코딩이 하나뿐이었다.**
- **고친 것**: 세 스크립트에 `sys.stdout/stderr.reconfigure(errors='replace')`.
  문자를 망가뜨릴지언정 검증기는 살린다 — `strict` 는 검증기를 망가뜨린다.
  `verify-frontmatter.sh --selftest` 신설 (4 케이스), `make frontmatter` 가
  `doc-refs` 와 같은 모양으로 selftest 를 먼저 돌린다.
- **케이스가 플랫폼 독립인 이유**: `PYTHONIOENCODING=ascii` 가 어느 OS 에서든
  같은 크래시를 낸다. Windows 전용 케이스였다면 CI 에서 안 돌아 또 같은 부류가
  됐을 것이다.
- **케이스를 쓰다 나온 것**: glob 검사의 grep 이 앵커가 없어 **자기 인자 문자열에
  매치됐고**, `verify-frontmatter.sh` 가 자기 검사만 면제받았다. 이 루프가 처음
  잡은 것이 자기 자신의 거짓말이다. 그리고 열거 대신 glob 으로 쓴 덕에
  `verify-benches.sh` 가 딸려 나왔다 — 손으로 골랐으면 둘만 고쳤다.
- **회차**: 3 (해결됨)

## 2026-08-12 — `make verify` 가 Windows 에서 한 번도 완주한 적이 없다

- **어디**: `Makefile:63` (`TRIGGER_LANGS`) · `scripts/verify-doc-refs.sh:240`
  (`EXCLUDE` 대조) · `plugins/harness-core/scripts/verify-gh-account-guard.sh` ·
  `plugins/harness-core/scripts/verify-large-file-veto.sh`
- **무슨 일**: 위 인코딩 건을 고치고 나니 그 뒤가 보였다. 셋 다 오탐이다.
  1. `TRIGGER_LANGS ?= 한국어|Korean` 이 Windows 네이티브 `make.exe` 를 지나며
     `?쒓뎅?` 로 깨져, 한국어 트리거를 멀쩡히 가진 스킬 5개가 "없음" 으로
     실패한다. bash 에서 같은 값을 직접 넣으면 11 / 11 통과다.
  2. `os.path.join(repo, 'evals/prose-corpus.md')` 는 `\` 와 `/` 를 섞어 만드는데
     `glob` 은 `\` 로만 준다. 문자열 비교라 `EXCLUDE` 가 안 맞고, **일부러 제외한
     `prose-corpus.md`** 가 6건 실패로 올라온다. 상대 링크 해석도 같은 이유로 4건.
  3. 훅 검증기 둘이 POSIX 를 가정한다 — `ln -s` 가 Windows 에서 복사본이 되고
     (`large-file-veto` 의 심링크 케이스), `gh` 계정 케이스가 8건 실패한다.
  4. **`verify-benches` 는 통과한다. 다만 수십 분이 걸린다.** 오래 도는 것을
     정지로 두 번 오진했고, 그 오진 과정이 이 항목에서 제일 쓸모 있는 부분이다.
     - 45초 캡에서 `bench-guards.sh` 가 rc=124 → *"반환하지 않는다"* 고 적었다.
       300초를 주니 **rc=0, 2분 31초** (user 39s / sys 63s).
     - 그다음 `bench-claims.sh` 가 300초 캡에서 rc=124 이고 CPU 를 거의 안 써서
       (user 0.24s) *"느린 게 아니라 멈춰 있다"* 고 적었다. 끝까지 두니
       **`PASS bench-claims produces a flag count` · `claim corpus yields 143
       tokens`** 로 통과했다. CPU 사용량이 0 에 가까운 것은 Git Bash 에서
       손자 프로세스의 rusage 가 안 잡히기 때문이지, 멈춘 증거가 아니었다.
     - 최종: `make verify-benches` **14 / 14 통과**. Windows 에서 `make verify` 를
       막는 것은 위 1·2·3 의 오탐뿐이고, 여기엔 정지가 없다.
     - **교훈**: `timeout` 이 준 rc=124 는 *"이 캡 안에 안 끝났다"* 는 뜻이지
       *"안 끝난다"* 가 아니다. 캡을 근거로 쓸 거면 캡 값을 문장에 같이 적어야
       하고, 그러면 이 오진은 쓰는 순간 눈에 보인다.
- **왜 안 적고 지나갈 수 없나**: 오탐은 `ADR-0003` 이 말한 **꺼지는 검사**다.
  지금 이 저장소를 Windows 에서 여는 사람에게 `make verify` 는 이미 꺼져 있다 —
  빨간불이 기본값이면 아무도 안 본다. 여기에 벤치가 수십 분을 먹는 것이 겹치면
  기다릴 이유까지 없어진다.
- **왜 이번에 안 고쳤나**: 인코딩 건과 원인이 다르고 (경로 처리·make 의 env 전달·
  POSIX 가정), 각각 고치는 방법도 다르다. 하나의 PR 로 묶으면 어느 것이 무엇을
  고쳤는지 못 읽는다. 그리고 `CLAUDE.md` §7 대로 **두 번째 발생을 기다릴 이유가
  있는지** 부터 봐야 한다 — 이 저장소가 Windows 를 지원 대상으로 삼는지가 아직
  어디에도 적혀 있지 않다. 그 결정이 먼저다.
- **결정이 필요한 것**: 지원 플랫폼을 `docs/agent-layer.md` 에 명시한다. 대상이
  macOS·Linux 뿐이라면 위 셋은 버그가 아니라 범위 밖이고, `make verify` 가
  Windows 에서 그렇게 말해야 한다. 대상이면 셋 다 고칠 일이다.
- **회차**: 1

## 2026-08-12 — [닫힘] Windows 를 지원 대상으로 정하고, 34건을 고쳤다

- **닫는 항목**: 바로 위 *"`make verify` 가 Windows 에서 한 번도 완주한 적이
  없다"* (1회차). 결정을 요청했고 **"윈도우에서도 동작해야지"** 로 답이 왔다.
  그래서 제안이 아니라 구현으로 닫는다. 지원 플랫폼은 `docs/agent-layer.md` §4 에
  적었다 — macOS · Linux · Git Bash 위의 Windows.
- **원인은 넷, 케이스 실패는 34건**: jq 의 CRLF 15 (gh-account-guard 8 +
  harnessctl 매니페스트 왕복 7), 경로 구분자 10, `make.exe` 재인코딩 5,
  `ln -s` 4.
- **가장 중요한 것 하나**: 그중 **하나는 테스트 문제가 아니라 가드가 열린
  것이었다.** Windows 의 `jq` 는 CRLF 를 출력하고 `$(...)` 는 개행만 지우고 CR 을
  남긴다. 그래서 `tool="Bash<CR>"` 같은 값이 생기는데, **부분일치는 계속 맞고
  정확비교만 깨진다** — 훅이 틀리는 게 아니라 조용해진다. `gh-account-guard` 가
  막아야 할 push 를 통과시켰다. 45 케이스 중 8개가 그것을 잡고 있었는데 아무도
  Windows 에서 돌리지 않았다 — **가드 실패가 보인 유일한 이유가 그 8개다.**
  "진짜 버그 1건 + 환경 잡음 33건" 으로 읽고 싶어지지만, 정직한 셈은 어느
  환경도 물어본 적 없는 결함 34건이다.
- **고친 것 (원인별)**:
  1. **`.gitattributes` 신설, `eol=lf`.** `core.autocrlf=true` 가 기본이라
     체크아웃이 전부 CRLF 였다. bash 는 CR 을 공백으로 안 보므로 줄 끝 CR 이 마지막
     토큰 **안으로** 들어간다 — `BOTH="a b"` 가 `a b<CR>` 를 대입하고, heredoc 은
     그 CR 을 생성 파일에 그대로 쓴다. 블롭은 이미 LF 였으므로 내용 변경은 0이다.
  2. **훅 5개 · `bin/` 둘 · `_verify-lib.sh` 에 CR 제거 jq 래퍼.** `jq() { local rc; command jq "$@" | tr -d '\r';
     rc=${PIPESTATUS[0]}; return "$rc"; }`. `PIPESTATUS` 로 종료 코드를 보존한다
     (`jq -e` 를 쓰는 호출자가 있다). **플랫폼 분기를 쓰지 않았다** — `tr` 은 LF 에서
     무해하고, `case $(uname -s)` 은 §4 가 금지하는 "한 환경에서만 도는 줄"이다.
  3. **`verify-doc-refs` 경로 구분자 2곳.** `links_resolve_here` 가
     `'/declarative/' not in path` 로 판정했는데 Windows 의 path 는 `\` 라 면제가
     한 번도 안 걸렸다. 일부러 제외한 payload 를 검사해 **맞는 링크 4개를 깨졌다고**
     보고했다. `EXCLUDE` 도 같은 이유로 `prose-corpus.md` 6건. 양쪽을 정규화했다.
  4. **`TRIGGER_LANGS` 를 `.claude/trigger-langs` 파일로.** Windows `make.exe` 가
     recipe 텍스트를 ANSI 코드페이지로 재인코딩해서 `한국어` 가 mojibake 로 도착했고,
     그 마커를 멀쩡히 가진 스킬 5개가 실패했다. 값이 재인코딩될 수 있는 프로세스
     경계를 아예 건너지 않게 했다.
  5. **`ln -s` 두 갈래.** 심링크가 *수단*인 곳(`verify-gh-account-guard` 의 최소
     PATH)은 exec 래퍼로 바꿨다 — Git Bash 의 `ln -s` 는 rc=0 을 내고 **복사본**을
     남기며, `command -v grep` 이 주는 `/usr/bin/grep` 은 디스크에 `grep.exe` 로만
     있어서 그 복사본은 빈 파일이었다. 심링크가 *주제*인 2건은 `skip_case` 로
     보고한다.
- **훅만으로는 절반이었다**: `harnessctl` 도 매니페스트를 jq 로 읽는다. 방금 깐
  파일을 "없다"고 보고하고, uninstall 이 아무것도 못 지웠다. `verify-install` 이
  7건으로 잡아냈다 — 검사가 제 일을 한 것이다. 그래서 래퍼는 `_verify-lib.sh` 에도
  넣어 (repo 전용 검증기는 `_check-lib.sh` 를 통해 상속) 한 군데서 끝냈다.
- **회귀 케이스를 `verify_begin` 에 넣은 이유**: 훅마다 자동으로 돌아서, 새 훅이
  래퍼 없이 도착할 수 없다. 이건 실제로 push 를 통과시킨 결함이라 관습이 아니라
  단언이어야 한다. 그리고 그 케이스가 처음 한 일이 **내가 만든 상호작용 버그를
  잡는 것**이었다 — `jq` 를 셸 함수로 만들면 `command -v jq` 가 경로 대신 함수명을
  답한다. `type -P` 로 고쳤다.
- **`skip_case` 를 왜 만들었나**: 심링크 케이스를 조용히 빼면 총계가 계속 그 케이스가
  돌았다고 주장한다. 건너뛴 것은 세고 이름을 적는다. 그리고 그 2건은 대체가 불가능하다
  — `ln -s` 가 복사본을 남기면 "40MB 심링크는 통과" 케이스가 **"40MB 일반 파일은 통과"**
  를 주장하게 되고, 그건 훅이 해야 할 일의 정반대다.
- **측정에서 세 번 틀렸고, 그게 이 항목의 재사용 가능한 부분이다**:
  - `timeout` 의 rc=124 를 "안 끝난다"로 두 번 읽었다. 캡을 늘리니 둘 다 반환했다.
  - 출력의 mojibake 를 값의 손상으로 읽었다. bash 직행 경로의 값은 온전했고 깨진
    것은 내 터미널의 디코딩이었다. **make 경로만 진짜로 깨져 있었다.**
  - `grep -cU $'\r'` 로 CRLF 를 세다가 모든 줄이 CR 이라는 오탐을 얻었다. python 의
    `count(b'\r\n')` 이 진실이었다. **탐지기부터 검증해야 한다.**
- **회차**: — (1회차 항목을 닫는 구현)

## 2026-08-12 — 총계는 검사받는데 그 총계를 이루는 내역 행은 아무도 안 본다

- **어디**: `scripts/verify-check-total.sh:44-48` · `README.md` 검증 표 · `README.ko.md`
  `### 1. make verify` 표
- **무슨 일**: `make verify-all` 이 문서의 692 와 실제 705 의 불일치를 잡아냈다 —
  검사는 제 일을 했다. 그런데 고치려고 표를 열어 보니 **내역 행끼리도 이미 어긋나
  있었다**. 영문 README 는 `7 hook verifiers 248`, `docs/agent-layer.md` §4 는 같은
  항목을 `250`. 영문 표의 행을 다 더하면 690 인데 그 표의 `**Total**` 행은 692 를
  주장하고 있었다. 즉 **표가 자기 자신과 맞지 않은 채로 발행돼 있었다.**
- **왜 안 걸렸나**: `verify-check-total.sh` 는 배지와 `| **Total** |` / `| **합계** |`
  행, 그리고 §4 의 `total of N` 만 grep 한다 (44-48행). 총계 한 줄만 맞으면
  통과이므로, 내역 행은 몇이든 상관이 없다. 총계를 손으로 고치는 동안 행은
  그대로 두는 것이 정확히 이번에 일어난 일이고, 그렇게 두 번 하면 행은 총계와
  영구히 갈라선다.
- **왜 지금 안 고쳤나**: §7 의 2회 게이트. 이번이 1회차다. 그리고 현재 증상은
  이번 커밋으로 해소됐다 — 양쪽 README 의 행 합이 각각 705 로 총계와 일치함을
  awk 로 확인했다.
- **후보 대책 (2회차 도달 시)**: `verify-check-total.sh` 에 세 번째 질문을 붙인다 —
  *"표의 내역 행을 더하면 그 표의 총계가 되는가"*. 양쪽 README 를 다 읽어야 하고
  (한글 표의 머리글은 `대상`/`케이스`), `**39** + selftest **7**` 처럼 한 행에
  숫자가 둘인 형식을 다뤄야 한다. 파일 목록은 하드코딩 말고 glob.
- **회차**: 1

## 2026-08-12 — init 이 죽으면 shim 이 안 써지고, 그래서 복구 명령이 존재하지 않는다

- **어디**: `install.sh:138-139` (step 4 의 die) 대 `install.sh:200-238` (step 5b 의
  shim) · `install.sh:134` 의 die 문구 · `plugins/harness-core/bin/harnessctl:600-603`
- **무슨 일**: 원격 리눅스 머신에서 `install.sh` 를 curl 파이프로 실행. 플러그인
  넷은 붙었고 step 4 에서 `~/.claude/settings.json is not a JSON object` 로 죽었다.
  이어서 `harnessctl init --scope project` 를 치니 `command not found`. 두 에러가
  별개로 보이지만 **한 사슬이다** — step 4 의 die 가 step 5b 앞에서 프로세스를
  끝내므로, init 이 실패한 설치는 **복구에 쓸 명령 자체를 남기지 않는다.**
- **더 나쁜 부분**: `install.sh:134` 의 (다른) die 문구가 바로 그 없는 명령을
  시킨다 — "run 'harnessctl init --scope $SCOPE' yourself". 새 세션의 Bash tool
  PATH 에서는 맞는 말이지만 사용자가 앉아 있는 터미널에서는 아니고, 문구는 그
  구별을 안 한다.
- **두 번째 결함**: `harnessctl:602` 의 `jq -e 'type == "object"'` 은 **서로 다른
  네 실패를 한 문구로** 뭉갠다. 실측한 rc: 빈 파일 4, `null` 1, `[]` 1, 문법 깨짐
  (`{"a":1,}`) 5. 그런데 문구는 "arrays, scalars and null" 셋만 대고 가장 흔한
  **빈 파일 / 깨진 JSON** 은 이름조차 안 댄다. 고치는 방법도 없다. `doctor` 는
  이 둘을 `jq empty` 로 구분해 "settings.json is broken JSON" 이라고 제대로
  말하지만 (`235행`), 그 시점의 사용자는 doctor 를 부를 수 없다 — shim 이 없으니까.
  **진단을 아는 코드가 저장소에 있는데 도달할 수 없는 곳에 있다.**
- **왜 안 잡혔나**: `verify-install.sh` 에 install.sh 절이 있지만 **성공 경로만**
  시험한다. step 4 가 실패한 뒤 install.sh 가 무엇을 남기는지 보는 케이스가 없다.
- **후보 대책 (2회차 도달 시)**: (1) shim 작성을 step 4 **앞으로** 옮긴다 — shim 은
  init 의 산출물에 의존하지 않고 플러그인 캐시만 읽으므로 순서를 바꿔도 잃는 게
  없다. (2) 602행을 `jq empty` 실패(깨짐·빈 파일)와 `type != "object"` 로 나누고
  각각에 백업 후 복구 한 줄을 붙인다. (3) `verify-install.sh` 에 실패 경로 케이스:
  깨진 settings.json 을 놓고 돌려 **shim 이 남는가**.
- **관련**: 8-11 "제거 안내가 넷 중 하나만 말했다" 와 같은 미덕(종료 문구가
  사용자를 실제로 내보내는가)이지만 결함이 다르다 — 그건 불완전한 목록, 이건 틀린
  원인. 각각 1회차로 센다.
- **회차**: 1

## 2026-08-12 — install.sh 가 jq 를 직접 깐다 (앞 두 항목의 절반만 닫힘)

- **어디**: `install.sh` (0단계 `ensure_jq`) · `scripts/verify-install.sh` §13 · README 양쪽
  요구사항 절 · `docs/agent-layer.md:11,134,386`
- **무슨 일**: 원격 머신의 실패 원인이 jq 미설치였고, 그 머신에는 sudo 가 없었다.
  설치기가 pinned jq(1.8.2)를 `~/.local/bin` 에 받아 **이 저장소에 커밋된 SHA-256**
  과 대조하고, 어긋나면 실행하지 않는다. 해시를 릴리스의 `sha256sum.txt` 에서
  받아오지 않은 이유: 파일과 같은 곳에서 온 해시는 전송 중 손상만 증명하고 그건
  TLS 가 이미 한다. 검사는 132 → 151 assertion (총계 705 → 724).
- **닫히지 **않은** 것**: 바로 앞 항목의 `command -v jq` 결함은 그대로다. 이 변경은
  가장 흔한 *발동 조건*을 없앨 뿐, `harnessctl` 을 직접 부르거나 shim 으로 부르면
  여전히 죽은 프리플라이트를 지나 엉뚱한 진단이 나온다. **2회차 항목은 열려 있다.**
- **측정에서 또 틀렸다 — 같은 교훈의 3회차다**: 새 케이스 19개를 변이로 검증했더니
  5건 중 4건이 "아무것도 안 잡힘" 으로 나왔다. 케이스가 아니라 **변이 스크립트가
  고장**이었다 — `perl -0pi -e "s/\Q$from\E/..."` 에서 perl 이 패턴 안의 `$got` /
  `$want` 를 **자기 변수로 보간**해 빈 문자열로 만들었고, 치환은 한 번도 일어나지
  않았다. 고친 판본은 **변이가 실제로 적용됐는지(`cmp`)를 먼저 단언**한다. 이 기록에
  이미 있는 "탐지기부터 검증해야 한다"(CRLF 오탐)의 세 번째 사례다.
- **그리고 변이가 진짜 구멍 하나를 잡았다**: PATH export 케이스가 `jq-1.8.2` 를
  grep 했는데 그 문자열은 export 와 무관한 `say` 줄에도 있어서, export 를 지워도
  초록이었다. 진짜 성질은 *"자식 프로세스가 jq 를 찾는가"* 이므로, 캐시에 가짜
  harnessctl 을 놓고 `command -v jq` 결과를 보고하게 바꿨다. 7개 변이 전부 잡힌다.
- **덤으로 발견, 안 고쳤다 (1회차)**: `install.sh:52` 의 `sed -n '2,25p'` 가 헤더보다
  두 줄을 더 읽어서 `--help` 출력 끝에 **`set -uo pipefail` 이 찍힌다.** 다섯 릴리스를
  살아남은 그 헤더 버그와 같은 자리다. 이번 변경 전부터 있던 것이고 요청 범위 밖이라
  §3 대로 손대지 않았다. 고치면 `'2,23p'`.
- **회차**: — (구현 항목)

## 2026-08-12 — [닫힘] jq 가드가 이제 실패할 수 있다 (2회차 마감)

- **어디**: `bin/harnessctl` · `bin/harness-log` · `scripts/_verify-lib.sh` · 훅 5개 ·
  저장소 전용 `scripts/{verify-install,verify-inventory,bench-guards}.sh`
- **무엇을 했나**: `command -v jq` → `type -P jq`, 전부. 죽어 있던 것은 셋뿐이었지만
  **여덟 곳을 다 바꿨다** — 훅 5개는 가드가 래퍼보다 *위에* 있어서 우연히 살아 있었고,
  저장소 전용 셋은 `_check-lib.sh` 를 source 하기 *전에* 가드를 둬서 살아 있었다.
  줄 순서에 기대는 불변식은 불변식이 아니다. 한 줄만 옮기면 조용히 다시 죽는다.
- **프리플라이트가 살아나면서 죽은 코드가 하나 생겼다**: `doctor` 의 `bad "jq missing"`
  분기는 이제 도달할 수 없다 (전역 프리플라이트가 먼저 멈춘다). 그 분기가 실행됐던
  유일한 이유가 프리플라이트가 죽어 있어서였고, 그래서 `ok jq ` 라는 빈 버전이
  찍혔다. 지웠고, jq 없는 사용자가 받는 문구를 케이스로 고정했다.
- **검사**: `verify_begin` 에 훅마다 자동으로 도는 케이스를 더해 (신규 6 — 훅 5 +
  harness-log) 새 훅이 이 결함을 갖고 도착할 수 없게 했다. `verify-install` 에는
  동작 케이스 8건 — jq 없는 PATH 로 세 실행파일을 돌려 **무슨 말을 하는지** 본다.
  151 → 159, 훅 255 → 260, harness-log 45 → 46, 총계 724 → 738.
- **변이 6/6**: 여덟 파일 중 어느 하나를 `command -v` 로 되돌리면 반드시 빨개진다.
- **그리고 검사가 또 자기 자신을 잡았다** — 이 기록에 세 번째로 적는 그 부류다.
  구조 검사의 grep 패턴(`command -v jq >/dev/null`)이 **그 패턴을 담은 자기 줄에**
  매치돼서 `verify-install.sh` 를 offender 로 신고했다. 주석 제거로 막으려다,
  `verify-frontmatter` selftest 가 이미 같은 문제를 **앵커**로 풀어 뒀다는 걸 보고
  (`^sys\.stdout\.reconfigure`) 같은 방식을 썼다: `^[[:space:]]*\(if ! \)\?command -v jq`.
  가드는 문장을 여는 자리에만 오고, 주석과 grep 패턴은 안 온다.
- **회차**: 2 — 닫힘. 남은 1회차 둘(shim 순서, `--help` 헤더 범위)은 그대로 열려 있다.

## 2026-08-12 — ai-attribution-guard 가 마크를 *찾는* 명령을 막았다

- **어디**: `plugins/harness-core/hooks/ai-attribution-guard.sh` (Bash 명령 전체 스캔)
- **무슨 일**: 커밋 본문에 attribution 이 없는지 **확인하려고** 돌린
  `git log --format='%B' main..HEAD | grep -icE 'co-authored-by|...|<AI 푸터 이모지>'`
  가 차단됐다. 훅은 명령 문자열에서 마크를 찾을 뿐, 그 마크가 *쓰이는지* 아니면
  *검사되는지* 를 구분하지 않는다. 읽기 전용 명령이었고 커밋도 PR 도 만들지 않았다.
- **왜 사소하지 않은가**: 차단 문구가 시키는 회피법("트레일러를 지워라")이 이 경우엔
  해당이 없다. 무엇을 하라는 건지 알 수 없는 차단은 훅을 끄고 싶게 만든다.
- **왜 지금 안 고치나**: 1회차다. 그리고 고치는 게 간단하지 않다 — "검사 중"과
  "작성 중"을 가르려면 `grep` 인자 안인지를 봐야 하는데, 그 판별을 잘못하면
  **가드가 막아야 할 것을 통과시킨다.** 우회는 사소하다 (이모지를 명령에 안 쓰면 된다).
- **후보 대책 (2회차 도달 시)**: 차단 문구에 한 줄 — *"검사하려던 것이라면 마크를
  명령에 직접 쓰지 말 것"*. 판별 로직을 건드리는 것보다 안전하고, 실제로 막힌
  사람에게 필요한 정보는 그것뿐이다.
- **회차**: 1

## 2026-08-13 — 프래그먼트의 스칼라는 이름으로 박혀 있어 새 스칼라가 무검사로 들어온다

- **어디**: `scripts/verify-install.sh:204` (그리고 214–228) ·
  `plugins/harness-core/declarative/settings-fragment.json`
- **무슨 일**: 프래그먼트에 두 번째 스칼라(`terminalProgressBarEnabled: true`)를
  추가했다. `make verify` 는 738/738 전부 통과했는데, **그 통과가 새 키가 실제로
  써진다는 증거가 아니었다.** 검사는 `jq -r '.includeCoAuthoredBy'` 로 스칼라를
  *이름으로* 확인한다. 병합 로직 자체는 `$frag.scalars` 를 순회하는 제네릭이라
  동작했지만, 만약 조용히 누락됐어도 초록색이 그대로 떴을 것이다. 네 속성
  (설치·영수증 기록·대칭 제거·기존 값 보존)을 전부 **손으로** 확인해야 했다.
- **왜 사소하지 않은가**: §4 의 표가 이미 판정을 내려 둔 부류다 — *"누락: 봐야 할
  것을 안 본다 → 먼저 설계로 막아라. 목록을 하드코딩하지 말고 glob 하라."*
  `context-budget` 의 파일 목록이 glob 인 것과 같은 이유다. permissions 쪽은
  이미 프래그먼트의 배열을 읽어서 대조하는데(이 기록 717행), **스칼라만 이름으로
  박혀 있어 비대칭이다.**
- **왜 지금 안 고치나**: 1회차. 그리고 이번 변경은 데이터 한 줄짜리라, 검사를
  고치는 일을 같은 PR 에 섞으면 §6 의 "구조 변경과 내용 추가는 분리한다" 를
  어긴다.
- **후보 대책 (2회차 도달 시)**: `verify-install.sh` 가 프래그먼트의
  `.scalars | keys[]` 를 순회하며 (a) 설치 후 그 값이 들어갔는지 (b) 영수증에
  기록됐는지 (c) 제거 후 사라졌는지를 돌린다. 그러면 스칼라를 추가하는 순간
  케이스 수가 저절로 늘고, 새 스칼라가 무검사로 들어올 길이 없어진다.
- **회차**: 1

## 2026-08-13 — 헤더를 줄 번호로 자르는 `--help` 가 또 어긋났다 (2회차)

- **어디**: `plugins/harness-core/bin/harnessctl:58` (`sed -n '2,24p'`) ·
  같은 패턴이 `install.sh:56` (`2,25p`) · `bin/harness-log:52` (`2,30p`)
- **무슨 일**: 프래그먼트에 스칼라를 추가하면서 harnessctl 헤더에
  `#   terminalProgressBarEnabled` 한 줄을 넣었다. `--help` 는 헤더를
  **고정 줄 번호로** 잘라 만들기 때문에 범위가 한 줄 밀렸고, 마지막 줄
  `Requires bash 3.2 (stock macOS) and jq.` 가 **조용히 사라졌다.** 에러도
  경고도 없고 `make verify` 는 738/738 초록이었다. `--help` 를 눈으로 보고서야
  발견했다.
- **1회차는 어디였나**: 위 *"install.sh 가 jq 를 직접 깐다"* 항목의 **덤으로
  발견, 안 고쳤다 (1회차)** — `install.sh:52` 의 `sed -n '2,25p'` 가 헤더보다
  두 줄을 **더** 읽어 `set -uo pipefail` 이 `--help` 끝에 찍히던 건이다.
  그때는 너무 많이 읽었고 이번엔 너무 적게 읽었지만, **원인은 하나다 —
  헤더의 끝이 헤더 밖의 줄 번호에 적혀 있다.** 헤더를 건드리는 사람은 그
  숫자가 있다는 걸 모른다.
- **왜 검사가 못 잡나**: `verify-doc-commands` 는 서브커맨드와 롱 플래그를
  파서와 대조하지, `--help` 본문이 헤더 전체를 담았는지는 보지 않는다.
  세 파일 모두 같은 방식이라 다음 사람도 같은 자리를 밟는다.
- **이번에 한 것**: `2,24p` → `2,25p` 로 되돌렸다. 증상만 고친 것이고
  구조는 그대로다.
- **후보 대책 (2회차 도달했으므로 제안)**: 줄 번호를 없앤다. 헤더 끝에
  `#` 만 있는 빈 주석 줄 같은 앵커를 두고 `sed -n '2,/^$/p'` 류로 자르거나,
  더 단순하게 **첫 비주석 줄 직전까지**를 출력한다
  (`sed -n '2,/^[^#]/p'` 후 마지막 줄 제거). 그러면 헤더 길이가 변해도
  따라온다. 세 파일이 같은 패턴이므로 한 번에 바꾼다.
  §6 대로 구조 변경이라 이 PR 이 아니라 별도 PR 이다.
- **회차**: 2 — PR 본문 `## Notes` 에 올렸다.

## 2026-08-13 — `gh pr merge` 는 자동 승인인데 `git merge` 는 물어본다

- **어디**: `plugins/harness-core/declarative/settings-fragment.json:51`
  (`permissions.allow` 의 `Bash(gh pr:*)`) 대 같은 파일 `:59`
  (`permissions.ask` 의 `Bash(git merge:*)`). 배열이라 항목이 늘면 줄이 밀리므로
  키 이름을 함께 남긴다 — 이 파일에 대한 앞선 두 항목(64행·308행)도 키로만
  적었고, 줄 번호는 그때 자리를 가리키는 용도다.
- **무슨 일**: 이번 세션에서 `gh pr merge 55 --squash --delete-branch` 가
  **프롬프트 없이 실행됐다.** `Bash(gh pr:*)` 가 allow 에 있어서 `gh pr view`·
  `gh pr create` 와 같은 취급을 받는다. 반면 로컬 `git merge` 는 ask 에 있다.
  **되돌리기 어려운 쪽이 자동이고 쉬운 쪽이 수동이다** — 기본 브랜치에
  스쿼시 머지하고 원격 브랜치까지 지우는 명령이 확인 없이 나간다.
- **왜 사소하지 않은가**: 이 저장소 사용자에게는 이미 *"CI 초록을 승인 대신
  쓰지 말 것, `gh pr merge` 전에 리뷰 유무를 확인할 것"* 이라는 명시적 지침이
  있다. 즉 사람은 이 명령을 위험하다고 판단하는데, 퍼미션 티어는 안전하다고
  말한다. 규약과 설정이 서로 다른 말을 한다.
- **왜 지금 안 고치나**: 1회차. 그리고 고치는 방식이 하나가 아니다 —
  `Bash(gh pr merge:*)` 만 ask 로 올리는 좁은 수정과, `gh pr` 전체를 ask 로
  내리는 넓은 수정이 있는데 후자는 `gh pr view`·`gh pr diff` 같은 읽기 전용
  호출까지 매번 묻게 만들어 §4 의 검증 루프를 느리게 한다.
- **후보 대책 (2회차 도달 시)**: allow 에 `Bash(gh pr:*)` 를 남기되 ask 에
  `Bash(gh pr merge:*)` 를 추가한다. 더 좁은 규칙이 이기는지를 먼저 확인해야
  하고 (확인 안 함), 이긴다면 이것이 최소 수정이다.
- **회차**: 1

## 2026-08-13 — 채택했다고 적힌 외부 계측기가 설치되어 있지 않다

- **어디**: `docs/agent-layer.md:74` (§3 "External instruments" 행의 ✅) 와
  같은 문서 `:164` (§4b 비용표의 `skill-creator` 112 / 10.9k 행). 판정의 출처는
  `docs/adr/0011-ecosystem-survey.md` §1 *"Adopt `skill-creator`"*.
- **무슨 일**: 외부 후보 11 개를 재검토하다 확인했다.
  `~/.claude/plugins/installed_plugins.json` 에 `skill-creator` 항목이 없다.
  설치된 것은 harness-core/dev/research/slides 와 superpowers 뿐이고,
  캐시(`cache/claude-plugins-official/skill-creator/unknown/`)에는
  `.orphaned_at = 1786230100534` (2026-08-09 08:01) 가 놓여 있다. **§3 의 ✅ 와
  §4b 의 "112 always-on" 은 이 기계에서 참이 아니다.**
- **왜 사소하지 않은가**: ADR-0011 의 Consequences 는 `run_eval.py` 를 쓰지
  않는다고 이미 취소선으로 적었다 (`scripts/bench-trigger.py` 가 대체). 그래서
  남은 채택 사유는 `analyzer`·`grader`·`comparator` 와
  `aggregate_benchmark.py` 뿐인데, §4b *"Not yet measured"* 는 `CLAUDE.md`
  원칙을 측정할 후보 자리로 바로 그 `grader` 를 지목해 둔 상태다. **한 번도
  쓰지 않은 도구가 조용히 빠졌고, 판정만 문서에 남았다.**
- **일반화**: 프로파일의 `dependencies` 에 적힌 외부 의존은
  `harnessctl doctor` 가 본다. 그런데 *"개발자만 쓰는, 배포하지 않는 외부
  계측기"* 는 확인 장치가 하나도 없다 — 문서의 ✅ 한 글자가 유일한 기록이고,
  그 글자는 아무도 검사하지 않는다.
- **왜 지금 안 고치나**: 1회차. 그리고 방향이 둘인데 어느 쪽인지 모른다 —
  다시 설치해 §4b 의 미측정 항목에 실제로 쓰거나, ✅ 를 내려 *"판정은 있고
  설치는 없음"* 으로 정직하게 적거나. `grader` 를 쓸 계획이 실제로 있느냐가
  정한다.
- **2026-08-13 분류**: 방향 결정 — ✅ 를 내린다 (같은 날 문서 PR, ADR-0011 에
  정정 블록). 재채택 조건을 함께 적었다: `claude plugin eval` 이 early access
  를 벗는 순간. help 는 이미 `--ablation with-without` 를 광고하는데 런타임은
  아직 차단 — 열리면 채택 사유였던 paired-run 이 네이티브로 대체된다.
- **회차**: 1 (해결됨 — 강등으로)

## 2026-08-13 — 천장 게이트가 서드파티 user-scope 플러그인을 못 본다

- **어디**: `scripts/context-budget.sh` 의 플러그인 합산부, 그리고 그것을
  서술하는 `docs/agent-layer.md` §4 표의 문구 — *"sums the **entire always-on
  footprint** (declarative plus plugins) per scope × profile"*.
- **무슨 일**: 외부 후보를 실측하려고 `watch@claude-video` (~98 tok) 와
  `taste-skill@taste-skill` (~1,697 tok, 스킬 13개) 를 user 스코프에 설치했다.
  이 저장소에서 도는 세션의 실제 always-on 은 7,934 + 1,795 = **~9,729 로
  9,000 천장을 넘었다.** 그런데 `make context-budget` 은 설치 전과 똑같이
  **7,934 / OK (1066 headroom)** 을 찍는다. **예산이 초과된 채로 게이트가
  초록이다.**
- **왜 사소하지 않은가**: 이 숫자가 실제로 내리는 결정은 §4 의
  *"There is ~1k of headroom, and it is there for the next one thing"* 이다.
  그 1k 는 이미 서드파티 플러그인 하나가 먹었는데 표는 아직 남았다고 말한다.
  즉 §4 의 **"entire always-on footprint" 가 실제보다 넓은 주장**이다 —
  스크립트가 세는 것은 *우리* 플러그인뿐이고, `claude plugin details` 로
  세면서도 설치된 목록이 아니라 이름 목록을 돈다.
- **왜 지금 안 고치나**: 1회차. 그리고 고칠 곳이 자명하지 않다. 천장은
  *하네스가 배포하는 것*에 대한 게이트여야 맞고, 사용자가 뭘 깔았다고 CI 가
  빨개지는 것은 틀렸다. 그러면 고칠 대상은 게이트가 아니라 **문구**(§4 의
  "entire") 이거나, 아니면 `harnessctl doctor` 쪽에 *정보성* 실측(설치된 전체
  플러그인 합)을 따로 붙이는 것이다. 후자는 CI 에 없는 CLI 를 요구하므로
  `verify` 에는 못 들어간다.
- **2026-08-13 분류**: 같은 날 사용자 지시의 재검토(명시적 retro)로 §5 의 제안
  조건 성립. 대책 확정 — 둘 다 한다: 게이트(CI, 우리 지분)는 그대로 두고
  `harnessctl doctor` 에 설치된 *전체* 플러그인 합산을 정보성으로 추가하며,
  §4 의 "entire" 문구를 같은 PR 에서 좁힌다. 별도 PR 로 진행.
- **회차**: 1 — 제안함

## 2026-08-14 — §3b 의 Measured 열에 귀속 없는 남의 수치가 2칸 있다

- **어디**: `docs/agent-layer.md` §3b 표의 `Measured` 열 — `ponytail` 행과
  `codebase-memory-mcp` 행. 그리고 그 열을 경고하는 머리말 — *"Most of it is
  empty, and that means not yet measured — not no effect."*
- **무슨 일**: 후보 목록을 통째로 읽어 달라는 요청에 답하려고 열을 세어 보니,
  채워진 16칸 중 **6칸**이 남이 발표한 수치였다 — `CodeGraph`, `ponytail`,
  `codebase-memory-mcp`, `headroom`, `caveman`, `agentmemory`. 그런데 그중
  **4칸은 이미 Measured 열 안에서 귀속돼 있다**: `Their claim:` 이 셀 첫머리에
  붙거나(`headroom`, `caveman`, `agentmemory`) *"Others report"* 로 시작한다
  (`CodeGraph`). 표기가 없는 것은 **`ponytail` 의 LOC −54% 와
  `codebase-memory-mcp` 의 0.83 vs 0.92, 두 칸뿐이다.**
- **왜 사소하지 않은가**: 머리말이 경고하는 것은 *빈 칸*(미측정)뿐이고,
  **채워진 칸의 출처**는 아무도 경고하지 않는다. 그리고 하필 미귀속인 두 칸이
  이 표가 실제로 내리는 결정 — "무엇을 채택할까" — 을 끌고 가는 두 칸이다.
  `codebase-memory-mcp` 를 *top candidate* 로 만든 근거는 통째로 남의 31개
  저장소 실험이고, `ponytail` 행은 §4b 가 베낄 설계로 지목한 근거다. 우리
  저장소의 `ARTIFACTS.md` 가 요구하는 *모든 수치는 그것을 낳은 run 으로
  추적된다* 를 §3b 자신이 이 두 칸에서 어기고 있다.
- **최초 기록의 정정 (PR #61 리뷰, 2026-08-15)**: 이 항목은 원래 *"채워진 16칸
  중 절반 이상"* 이 남의 수치이고 귀속 표기는 *"본문 문장"* 에만 있다고 적었다.
  둘 다 틀렸다 — 실제로는 6칸이고, 표기는 본문이 아니라 **Measured 셀 안에**
  있다. 개수를 3배로 잡은 탓에 대책이 "27행 전수 작업, 2회차 대기" 로 부풀었다.
  머지되면 append-only 라 못 고치므로 머지 전에 정정한다.
- **대책 (지금 가능)**: 두 칸에 표에 **이미 있는 관례**(`Their claim:`) 를 붙인다.
  새 규약도, 열 분리도, 27행 손질도 필요 없다. 새 행을 기록하는 것과는 다른
  단위이므로 별도 PR — 2회차를 기다릴 이유는 없다.
- **2026-08-15 해소**: 위 대책대로 `ponytail` 과 `codebase-memory-mcp` 두 칸에
  `Their claim:` 을 붙였다. 이제 남의 수치 6칸이 전부 셀 안에서 귀속된다.
  머리말은 손대지 않았다 — 여전히 *빈 칸*만 경고한다.
- **회차**: 1 — 해소됨

## 2026-08-14 — doctor composite 절이 프로젝트 스코프 enabledPlugins 를 안 센다

- **어디**: `plugins/harness-core/bin/harnessctl` doctor 의 always-on 절 —
  `USER_SETTINGS` 의 `enabledPlugins` 만 순회한다. 헤더는 "every enabled
  plugin" 이라고 말한다.
- **무슨 일**: PR #60 머지 리뷰에서 확인. 프로젝트 스코프로 enable 된
  플러그인(이 머신의 `ponytail`·`headroom` 이 실제 사례 — 다른 저장소에
  project 스코프로 설치돼 있다)은 그 프로젝트의 세션에 로드되는데 합산에서
  빠진다. #60 이 §4 에서 고친 것과 같은 종류의 과잉 주장이 더 작은 규모로
  새 코드에 들어갔다.
- **왜 지금 안 고치나**: 1회차이고 비차단 — 정보성 출력이며 주 사례(user
  스코프 설치)는 덮는다. 수정 방향은 둘: `--scope project` 일 때 프로젝트
  `.claude/settings.json` 의 enabledPlugins 를 합치거나, 헤더를 "every
  plugin enabled at user scope" 로 좁히거나. 전자가 맞아 보이지만 중복
  항목(user 와 project 에 같은 spec)의 이중 계산을 결정해야 한다.
- **회차**: 1

## 2026-08-15 — pr-review 의 더러운 트리 대비책이 리뷰를 불필요하게 깎는다

- **어디**: `plugins/harness-dev/skills/pr-review/SKILL.md:46` — *"When dirty,
  do not check out. Work from `gh pr diff` and the base files, and **say in the
  report that the surrounding context was not read**."*
- **무슨 일**: PR #61 리뷰 중. 작업 트리에 커밋 안 된 `.claude/harness-gaps.md`
  가 있어 이 절이 발동했는데, 스킬이 제시하는 선택지는 둘뿐이다 — 체크아웃
  하거나, 주변 문맥을 못 읽었다고 실토하거나. **세 번째 길이 있다**:
  `git worktree add <tmp> origin/<head>` 는 작업 트리를 건드리지 않으면서
  전체 문맥 열람과 `make verify-all` 실행을 둘 다 준다. 이번엔 그렇게 했고,
  그 덕분에 차단급 지적 두 건이 나왔다 — 하나는 `make verify-all` 을 PR head
  에서 직접 돌려야 확인되는 것이었다. 스킬 문구를 그대로 따랐다면 "문맥 못
  읽었음" 딱지가 붙은 반쪽 리뷰가 됐다.
- **왜 사소하지 않은가**: Step 3 은 *"Do not review from the diff alone"* 이라고
  선언해놓고, 바로 밑에서 흔한 상황(더러운 트리)에 대해 diff 만 보라고 한다.
  스킬이 자기 원칙을 스스로 면제하는 셈이고, 면제 조건이 "커밋 안 한 파일이
  하나라도 있으면" 이라 발동 빈도가 높다.
- **왜 지금 안 고치나**: 1회차. 그리고 worktree 가 항상 옳지는 않다 — 디스크와
  설정 초기화 비용이 있고, PR 브랜치가 이미 로컬에 있으면 불필요하다.
- **후보 대책 (2회차 도달 시)**: :46 을 3단으로 바꾼다 — 깨끗하면 `gh pr
  checkout`, 더러우면 `git worktree add` 로 분리된 트리, worktree 도 불가하면
  그때 diff-only 로 내려가고 그 사실을 보고에 적는다. "문맥을 못 읽었다" 는
  마지막 수단이지 더러운 트리의 기본값이 아니다.
- **회차**: 1

## 2026-08-15 — 이웃 플러그인의 깨진 훅을 하네스가 못 본다

- **어디**: `plugins/harness-core/bin/harnessctl` doctor — guards 절은 *우리*
  훅만 실제로 실행해 보고, plugins 절은 설치 여부와 버전만 본다. 등록된 훅의
  **명령이 실제로 resolve 되는지** 는 아무도 안 본다.
- **무슨 일**: Windows 세션에서 `headroom@headroom-marketplace` 가 enable 돼
  있었다. 그 플러그인의 `hooks.json` 은 PreToolUse(matcher `Bash|PowerShell`)
  에 `headroom init hook ensure` 를 맨 명령으로 등록하는데, 존재 확인 가드가
  없다. CLI 가 PATH 에 없어서 **Bash 툴을 부를 때마다** 127 로 죽고
  `headroom: command not found` 가 떴다. 사용자가 눈으로 알아채고 물어볼
  때까지 아무것도 이걸 말해주지 않았다.
- **왜 사소하지 않은가**: 이건 §3 훅 계약이 막으려는 바로 그 실패 —
  *"A missing hook must not block work"* — 인데 계약은 **우리 훅에만** 걸린다.
  이웃이 어기면 대가는 같은 세션이 치른다. 그리고 doctor 는 이미 서드파티
  위생을 한 줄 말하고 있어서(*"registered marketplaces no enabled plugin
  uses"*) 스코프 밖이라고 자르기도 애매하다.
- **왜 지금 안 고치나**: 1회차. 고칠 방향이 자명하지 않다 — 남의 플러그인
  훅을 doctor 가 검사하는 게 우리 일인지부터 결정 사항이고, 명령 resolve
  검사는 셸과 OS 마다 답이 다르다(이번 사례가 정확히 Windows 였다).
- **회차**: 1

## 2026-08-16 — `install.sh` 는 HOME 없이 `--help` 조차 못 낸다

- **어디**: `install.sh:33` — `BIN_DIR="${BIN_DIR:-$HOME/.local/bin}"` 가
  인자 루프보다 위에 있다. 그리고 `scripts/verify-install.sh` 의 `run_probe`
  는 `env -i` 를 쓰지 않아 이 상태를 만들어 보지 않는다.
- **무슨 일**: `uninstall.sh` 를 쓰면서 같은 줄을 그대로 베꼈고, 새 케이스를
  `env -i PATH=...` 로 돌리자 바로 터졌다 — `set -u` 아래에서 HOME 이 없으면
  `HOME: unbound variable` 로 죽고, **`--help` 도 못 나온다**. 컨테이너·cron·
  systemd 유닛이 실제 사례다. `uninstall.sh` 쪽은 해결를 인자 루프 아래로
  내리고 이름 붙은 에러를 주는 것으로 고쳤다.
- **왜 사소하지 않은가**: `--help` 는 "아무것도 안 하는 경로" 라서 가장 척박한
  환경에서 도는 것이 존재 이유다. 그리고 원인이 메시지에 안 나온다 — 사용자가
  보는 것은 스크립트 33번째 줄의 변수 이름 하나다.
- **왜 지금 안 고치나**: 1회차. 그리고 `install.sh` 수정은 이번 변경(제거
  경로 추가)과 다른 단위다 — §6 이 구조 변경과 내용 추가를 나누라고 한다.
- **후보 대책 (2회차 도달 시)**: `install.sh:33` 을 `uninstall.sh` 와 같은
  모양으로 — 선언은 `BIN_DIR="${BIN_DIR:-}"`, 해결은 인자 루프 뒤에서 HOME 을
  확인하고 이름 붙은 die. 그리고 `run_probe` 를 `env -i` 로 올려 이 성질이
  회귀하지 않게 한다. 후자가 없으면 같은 줄이 다시 돌아온다.
- **회차**: 1

## 2026-08-17 — 머지 전 새 스킬은 두 계측기에 보이지 않는다

- **어디**: `scripts/context-budget.sh` (플러그인 비용) · `scripts/bench-trigger.py`
  (라우팅) · `CLAUDE.md` §2b 가 요구하는 4단계
- **무슨 일**: `cross-model-review` 를 추가하면서 §2b 의 3번(`make bench-trigger`
  로 재고 §4b 에 기록)과 상시 컨텍스트 비용, 둘 다 **로컬에서 잴 수 없었다.**
  원인이 같다 — 두 계측기 모두 *설치된* 플러그인을 본다. 그런데 이 기계에
  등록된 마켓플레이스는 GitHub(`chpark-ML/agent-harness`) 하나뿐이라, 아직
  머지되지 않은 스킬은 캐시에 존재하지 않는다.
  - 비용 쪽은 `--require-plugins` 가 *"installed 1.0.4, tree 1.1.0 — measuring
    the OLD one"* 이라고 정확히 말해줬다. **이건 고장이 아니라 2026-08-11 항목의
    수정이 의도대로 동작한 것이다.** 조용히 옛 값을 내놓지 않았다.
  - 라우팅 쪽은 그런 안전장치가 없다. 그냥 돌렸으면 트리거돼야 할 6개가 전부
    깨끗한 0 으로 나왔을 것이고, 그건 §4b 가 정리해 둔 *계측기 실패* 아홉 건과
    같은 모양이다. 스크립트 머리말에 "이미 설치된 스킬은 대역으로 못 잰다" 고
    적혀 있어 알아챘다.
- **이번에 우회한 방법**: 스크래치 클론을 만들고 `SKILL.md` 를
  `.claude/skills/<name>/` 에 프로젝트 스킬로 세운 뒤 `--cwd` 로 겨눴다.
  한국어 트리거 파일럿(§4b)이 쓴 것과 같은 방법이다. 비용 쪽은 우회로가 없어
  CI 의 `context-budget-strict` 를 기다린다.
- **왜 지금 안 고치나**: 1회차다. 그리고 고칠 방향이 둘인데 어느 쪽인지 모른다.
  `bench-trigger.py` 가 "이 세트의 스킬이 설치돼 있지도, `--cwd` 아래 프로젝트
  스킬로 있지도 않다" 를 **먼저 확인하고 거부**하게 하거나 — 값이 싸고, §4 의
  "오탐을 내는 검사만 케이스를 갖춘다" 에도 걸리지 않는다 — 아니면 신규 스킬
  측정 절차를 `CONTRIBUTING.md` 에 한 문단으로 적거나. 앞쪽이 유력하지만 두 번째
  발생을 기다린다.
- **회차**: 1

## 2026-08-17 — §2b 스킬 번들은 스킬이 움직이는 공표 수치를 안 적는다

- **어디**: `CLAUDE.md` §2b (스킬 산출물 4개) · `CONTRIBUTING.md` "Adding a skill"
- **무슨 일**: `cross-model-review` (#66) 를 §2b 의 4단계대로 다 채웠는데도 두 번
  더 고쳐야 했다. 둘 다 목록에 없는 항목이다.
  - **검사 총계 771 → 774.** `SKILL.md` 하나를 더하면 `verify-doc-refs` 가 그것을
    두 번 센다 (문서로 한 번, instruction 파일로 한 번) 그리고
    `verify-frontmatter` 가 한 번 더. `make verify-all` 이 잡아줘서 머지 전에
    고쳤다.
  - **상시 컨텍스트 공표값 7,085 → 7,211.** 이건 **CI 가 돌기 전엔 알 수 없다**
    (바로 위 항목의 이유로 로컬 측정이 불가능하다). PR 을 한 번 올린 뒤 CI 수치를
    보고 4곳을 고쳐 다시 푸시하는 왕복이 강제됐다.
- **일반화하면 3회차다.** 같은 모양이 이 기록에 이미 둘 있다 — 2026-08-08
  *"배포되는 `bin/` 실행파일에는 산출물 규정이 없다"*, 2026-08-12 *"훅이 독립
  코퍼스 없이 배포됐다"* (§2 의 6개 항목에 `evals/incidents.sh` 가 없다). 셋 다
  **산출물 목록이 실제로 필요한 것보다 짧다** 이고, 셋 다 목록을 믿고 따른
  사람이 뒤늦게 발견했다.
- **주의**: 검사는 둘 다 잡아냈다 (`verify-check-total`, `context-budget` 의 공표값
  대조). 즉 이건 *놓친 검사*가 아니라 *예고되지 않은 작업*이다. 얻는 것이 왕복
  한 번 줄이는 정도라 크지 않다 — 그래서 승격이 아니라 후보로만 적는다.
- **후보 규약**: §2b 에 5번 항목. *"새 스킬은 공표 수치 둘을 움직인다: 검사 총계
  (+3, `SKILL.md` 하나당) 와 상시 컨텍스트 worst case. 후자는 CI 만 잴 수 있으니
  PR 을 올린 뒤 CI 수치로 4곳을 갱신하는 커밋이 하나 더 붙는다고 보고 시작하라."*
  `CONTRIBUTING.md` 의 같은 절에도 같은 문장.
- **왜 지금 안 고치나**: `CLAUDE.md` §5 가 하네스 변경은 기능 변경과 분리해
  배포하라고 하고, #66 은 방금 머지됐다. 그리고 §2 (훅) 와 §2c (실행파일) 도 같은
  문제를 안고 있어서, 셋을 한 번에 손보는 편이 낫다 — 이 항목만 고치면 다음
  사람이 §2 에서 같은 것을 다시 발견한다.
- **회차**: 3 (일반화 기준. §2b 단독으로는 1회차)
- **2026-08-17 해결됨**: 같은 날 4회차가 나와 셋을 한 번에 고쳤다. 공표 수치
  두 개는 세 목록이 공유하는 문제라 목록마다 반복하지 않고 §2d 한 절로 넣었다.

## 2026-08-17 — 스킬은 본문을 한 번도 실행해 보지 않은 채 배포된다

- **어디**: `CLAUDE.md` §2b (스킬 산출물 4개) · `evals/trigger/<name>.json` 이
  재는 것의 범위
- **무슨 일**: `cross-model-review` 는 §2b 를 다 채우고 라우팅 12/12 로 머지됐다
  (#66). 그 뒤 처음으로 본문을 실제로 끝까지 돌려봤더니 **Step 4 가 세 군데
  틀렸다.**
  - 전송 방법이 아예 안 된다. 본문은 diff 를 입력창에 타이핑한다고 전제하는데
    23 KB 는 타이핑이 불가능하고, 합성 `cmd+v` 는 붙여넣기를 일으키지 못한다.
    실제로 통한 것은 `.diff` 파일 첨부였다.
  - 입력창 확인 가드가 한 번만 읽는다. 붙여넣기 직후 화면은 **비어 있었고**
    몇 초 뒤에 내용이 들어왔다. 그 사이에 지시문을 쳐서 23 KB 중복이 만들어졌다.
    입력창도 응답과 똑같이 지연된다.
  - 응답 폴링의 "연속 두 번 일치" 가 부족하다. 두 번 다 *"Non-blocking
    findings"* 라는 제목에서 끝난 같은 정체 구간에 걸렸다. 스크린샷 대조가
    없었으면 **잘린 응답을 "지적 없음" 으로 보고**했을 것이다.
- **왜 아무 검사도 못 잡았나**: 트리거 eval 은 *스킬이 뜨는가* 만 잰다. 본문의
  절차가 도는지는 아무도 안 본다. 훅은 `verify-<name>.sh` 가, 실행파일은 자기
  검증기가 지키는데, **스킬 본문은 산문이라 지키는 것이 없다.**
- **일반화하면 4회차다.** 오늘 적은 *"§2b 스킬 번들은 공표 수치를 안 적는다"*
  와 같은 뿌리이고, 그 항목이 인용한 두 건(실행파일 규정 없음 · 훅 독립 코퍼스
  없음)까지 넷이다. 다만 **이번 것이 가장 무겁다** — 앞의 셋은 수치가 낡거나
  측정이 비는 문제였고, 이것은 *배포된 절차가 실행하면 실패한다* 이다.
- **후보 규약**: §2b 에 항목 하나 — *"머지 전에 본문을 한 번 끝까지 돌린다.
  대상은 아무 커밋 범위나 좋다. 돌려본 적 없는 절차는 검증된 적 없는 절차다."*
  값이 싸고(한 번 실행), 이번에 실제로 세 건을 잡았다.
- **왜 지금 안 고치나**: 이번 PR 은 스킬 본문 수정이고, §2b 개정은 하네스 규약
  변경이라 `CLAUDE.md` §5 가 분리하라고 한다. 그리고 §2 · §2b · §2c 세 목록을
  한 번에 손보는 편이 낫다는 판단은 그대로다 — 이제 그 PR 의 근거가 넷이다.
- **회차**: 4 (일반화 기준)
- **2026-08-17 해결됨**: §2b 에 항목 5 — 머지 전 본문 한 번 완주. 그리고 빠져
  있던 §3 인벤토리 행이 항목 4 로 함께 들어갔다. `CONTRIBUTING.md` 도 같이.

## 2026-08-17 — 스킬을 호출하면 트리가 아니라 낡은 캐시가 돈다

- **어디**: `plugins/harness-dev/.claude-plugin/plugin.json` (트리 `1.1.1`) 대
  `~/.claude/plugins/cache/agent-harness/harness-dev/1.1.0/` (설치본) ·
  `CLAUDE.md` §2b 항목 5
- **무슨 일**: `/harness-dev:cross-model-review` 를 불렀더니 **#68 이전 본문이
  로드됐다.** 세션에 들어온 Step 4 는 "diff 를 입력창에 타이핑한다 · 가드 2개"
  였다. 그런데 트리의 Step 4 는 #68 (`fix-cross-model-transport`) 로 이미
  "`.diff` 파일 첨부 · 가드 3개" 로 바뀌어 있다. 즉 **바로 그 전송 방법이 안
  된다고 판명나서 고친 본문 대신, 안 되는 쪽 본문이 실행됐다.**
- **버전은 올라가 있었다.** 트리 `1.1.1`, 캐시 `1.1.0`. §2 가 요구하는 버전 범프는
  지켜졌고, 이 기계가 `claude plugin update` 를 안 했을 뿐이다. 고장이 아니라
  캐시 갱신 시점 문제다 — 문제는 **그 사실을 아무도 말해주지 않는다** 는 것.
- **같은 날 확인: 밀린 것은 이 패치 하나가 아니었다.** `claude plugin list` 와
  트리를 대조하니 `harness-core` 가 **설치 1.13.0 대 트리 1.21.0**, 마이너 8개
  차이였다 (`harness-dev` 1.1.0/1.1.1, `harness-research` 와 `harness-slides` 는
  일치). 즉 이 기계는 **훅·스킬·스크립트를 통째로 옛 버전으로 돌리고 있었고**,
  그 상태로 여러 세션이 지나갔다. 처음 적을 때 근거로 삼은 패치 하나보다 훨씬
  크다 — 드리프트는 예외가 아니라 기본값에 가깝다.
- **그리고 이 대조는 손으로 해야 나온다.** `claude plugin list` 는 설치 버전만,
  트리는 트리 버전만 말한다. 둘을 나란히 놓는 것은 아무도 안 해준다.
- **같은 날 원인을 찾았다 — 이건 사용자 부주의가 아니라 `install.sh` 의 결함이다.**
  사용자가 "curl 명령으로 설치하면 다 업데이트돼야 하는 것 아니냐" 고 물어서
  확인했더니, 그 기대가 맞고 스크립트가 틀렸다.
  - `install.sh:213` 은 `claude plugin install` 을 쓴다. 이 명령은 **이미 설치된
    플러그인에 대해 버전을 비교하지 않고 그냥 넘어간다.** 실측:
    `✔ Plugin "harness-dev@agent-harness" is already installed (scope: user)`
    — 버전 문자열이 아예 등장하지 않는 존재 확인이고, 종료 코드는 0 이다.
    올리는 것은 별도 명령인 `claude plugin update <plugin>` 뿐이고, 이 이름은
    저장소 어느 안내에도 없다.
  - 그런데 `install.sh:203` 은 마켓플레이스는 **갱신한다**
    (`claude plugin marketplace update`). 조합이 최악이다 — 카탈로그는 1.21.0 을
    가리키는데 설치본은 1.13.0 에 머문다. 재설치가 "최신" 을 보여주면서 최신을
    주지 않는다.
  - **README 에 업데이트 절이 아예 없다.** 헤딩은 Quick start → Taking less than
    everything → Verify and undo → Requirements 로 이어지고, "최신으로 올리는 법"
    이 없다. 그래서 사용자가 도달하는 자연스러운 답 — *`One command. It installs
    everything.` 이라고 적힌 그 curl 을 다시 돌린다* — 이 플러그인 절반에 대해
    조용히 아무 일도 안 한다.
  - **`CLAUDE.md` §2 의 근거도 절반만 참이다.** "버전을 안 올리면 캐시가 그대로라
    사용자에게 아무것도 전달되지 않는다" 는 맞다. 하지만 올려도 전달되지 않는다 —
    `claude plugin update` 를 손으로 부른 사람에게만 간다. 범프는 필요조건이지
    충분조건이 아닌데 §2 는 충분조건인 것처럼 읽힌다.
- **`verify-install.sh` 가 이걸 못 잡는 이유**: §3 이 검사하는 것은 *멱등성* —
  `re-install changes nothing on disk`. 선언적 절반에는 옳은 기대지만, 플러그인
  절반에서는 **틀린 기대를 테스트가 못 박고 있는 셈**이다. 업그레이드 경로를 보는
  케이스는 없다.
- **분류 변경**: 처음엔 "캐시 갱신 시점 문제, 고장은 아니다" 로 적었다. 그건
  틀렸다. 배포된 스크립트가 자기가 하겠다고 적은 일을 안 하는 것이므로 **결함**
  이고, 회차 임계값을 기다릴 대상이 아니라 고칠 대상이다.
- **제안**: ① `install.sh:213` 에서 출력이 `already installed` 이면
  `claude plugin update` 로 이어간다 — `--ref` 로 고정한 설치를 풀어버리지 않도록
  주의가 필요하다. ② README 에 업데이트 절 신설, 재시작 필요 명시. ③
  `verify-install.sh` 에 업그레이드 경로 케이스. ④ `CLAUDE.md` §2 에 "범프는
  필요조건일 뿐" 한 줄.
- **2026-08-17 해결됨**: 넷 다 반영했다. `--ref` 우려는 구현 전에 측정으로
  걷어냈다 — 격리된 `CLAUDE_CONFIG_DIR` 에 v0.2.0 으로 고정 설치한 뒤 재실행
  경로를 그대로 밟았더니 `marketplace update` 는 고정을 유지했고,
  `plugin update` 는 `already at the latest version (1.11.0)` 로 답했다.
  "최신" 을 marketplace 기준으로 판정하므로 고정은 깨지지 않는다. 같은 클린룸에서
  `plugin install` 이 `is already installed` 로 무효화되는 것도 재현했다.
  `verify-install.sh` §12a 에 케이스 5개 (이미 설치됨 → update 호출 · 스코프
  전달 · **신규 설치는 update 를 부르지 않는다** · update 실패 시 중단 · 실패
  메시지가 플러그인 이름을 담는다). 공표 수치 둘이 따라 움직였다: 설치기
  assertion 192 → 197, 검사 총계 774 → 779.
- **왜 이게 날카로운가**: 같은 뿌리의 2026-08-17 첫 항목에서 `context-budget` 은
  *"installed 1.0.4, tree 1.1.0 — measuring the OLD one"* 이라고 정확히 경고했다.
  `bench-trigger.py` 는 머리말로 경고한다. **경고가 전혀 없는 경로가 스킬 호출
  자체이고, 하필 그것이 §2b 항목 5 가 "머지 전에 본문을 한 번 끝까지 돌린다" 고
  지정한 바로 그 경로다.** 시킨 대로 호출해서 돌리면 트리의 본문이 아니라
  캐시의 본문을 검증하게 된다 — 방금 고친 것을 검증했다고 착각하기 딱 좋다.
- **차이**: 첫 항목은 *머지 안 된 스킬이 안 보인다* 였고, 이것은 *머지된 수정이
  안 보인다* 다. 방향이 반대라 같은 항목으로 묶지 않는다.
- **왜 지금 안 고치나**: 1회차다. 그리고 고칠 자리가 저장소 밖일 수 있다 —
  플랫폼이 캐시/트리 불일치를 알려주는 것은 우리가 못 만든다. 우리 쪽에서 값싸게
  되는 것은 §2b 항목 5 에 한 줄 (*"호출은 캐시를 돈다. 트리 본문을 돌리려면
  프로젝트 스킬로 세우고 `--cwd` 로 겨눠라 — 첫 항목의 우회로와 같다"*) 이거나,
  `scripts/` 에 설치본과 트리의 `version` 을 대조하는 검사다. 후자가 유력하지만
  두 번째 발생을 기다린다.
- **회차**: 1

## 2026-08-17 — cross-model-review 가 싼 점검보다 비싼 점검을 먼저 시킨다

- **어디**: `plugins/harness-dev/skills/cross-model-review/SKILL.md` — Step 1
  (채널 프리플라이트) 과 Step 2 (범위 산출) 의 순서
- **무슨 일**: 본문은 Step 1 에서 브라우저를 열어 `chatgpt.com` 에 붙고, 그
  다음 Step 2 에서 `git merge-base` 로 범위를 만든다. 이번 호출은 `main` 위
  깨끗한 트리라 **범위가 비어 있었다.** 순서대로 따랐으면 리뷰가 성립하지 않는
  것이 확정된 상태에서 탭을 열고 서드파티 사이트에 접속한 뒤에야 그것을 알게
  된다.
- **작다**: 낭비되는 것은 탭 하나와 왕복 한 번이고, 잘못된 결과가 나오지는
  않는다. Step 1 의 근거("약속하기 전에 채널부터 본다")도 여전히 옳다 — 범위
  산출은 사용자에게 아무것도 약속하지 않으므로 먼저 해도 그 근거를 해치지 않는다.
- **이번에 우회한 방법**: Step 2 를 먼저 돌렸다. §5 가 말하는 *조용히 우회하기*
  에 해당하므로 여기 적는다.
- **후보 규약**: Step 1 과 Step 2 를 맞바꾸고, Step 1 첫 줄에 *"빈 범위면 채널은
  보지도 마라"* 를 넣는다.
- **회차**: 1
