# harness gaps — 원장

`CLAUDE.md` §5 의 "두 번 이상 발생하면 제안한다" 를 세기 위한 파일이다. 세션을
넘겨 발생을 세는 장치가 이것뿐이므로, **1회차도 적는다** — 1회차를 안 적으면
2회차가 영원히 1회차로 보인다.

한 항목: 날짜 · 파일:줄 · 무슨 일이 있었나 · 몇 회차인가. 2회차가 되면 그때
PR 본문 `## Notes` 로 올라가고, 고치는 것은 그다음이다.

---

## 2026-08-07 — `superpowers:finishing-a-development-branch` 가 위임을 받지 않는다

- **어디**: `docs/adr/0009-external-dependencies.md` — 갱신 절의 위임 판정
- **무슨 일**: ADR-0009 는 머지·브랜치 정리를 Superpowers 쪽에 남기고 우리는
  PR 생성까지만 한다고 정했다. 실측에서 "이 PR 머지하고 브랜치 정리해줘. squash
  로 넣고 원격 브랜치도 지워줘" 가 **세 번 다 `Bash` 로** 갔다. 위임한 상대가
  그 자리를 받지 않는다. 사용자는 규약 없는 맨 `git` 명령을 받게 된다.
- **왜 지금 안 고치나**: 1회차다. 그리고 고치는 방법이 둘인데 어느 쪽인지
  모른다 — 우리가 `pr-finish` 를 만들어 자리를 메우거나, 위임 자체를 철회하고
  `pr-create` 의 범위를 넓히거나. 둘 다 되돌리기 비싼 결정이라 두 번째 발생을
  기다린다.
- **2026-08-08 분류**: 대기 — 그리고 반대 증거가 하나 생겼다. 이번 세션에서
  "머지하고 retro" 에 이 스킬을 Skill 로 불렀고 정상 로드됐다. 원 관측은 *위임이
  Bash 로 샜다* 였는데, 명시적으로 부르면 동작한다. 2회차 조건을 "부르지 않았는데
  샜다" 로 좁힌다.
- **회차**: 1

## 2026-08-07 — 스킬 본문의 경로 오타는 조용히 죽는다

- **어디**: `plugins/harness-core/skills/pr-create/SKILL.md:90`
- **무슨 일**: Step 6 을 쓰면서 `.claude/harness-gaps.md` 를 `.claece/...` 로
  적었다. `[ -f ... ] && ...` 형태라 파일이 없으면 조용히 아무 일도 안 하고
  넘어간다 — 즉 **이 절이 영원히 안 도는데 아무도 모른다.** 같은 회 안에서
  발견해 고쳤다.
- **일반화**: 훅은 `verify-*.sh` 가 지키고 프론트매터는 `verify-frontmatter.sh`
  가 지키는데, **스킬 본문 안의 경로·명령은 아무도 안 본다.** `check-claims.sh`
  가 덱의 수치를 검사하듯, 스킬이 언급하는 경로가 실재하는지 훑는 검사가 같은
  종류의 빈틈을 덮는다.
- **왜 지금 안 고치나**: 1회차. 다만 이건 값이 싼 검사라 2회차가 나오면 바로
  만든다.
- **2026-08-08 분류**: 2회차가 아래 *"문서가 가리키는 내부 참조는 아무도 안 본다"*
  로 섰고 `scripts/verify-doc-refs.sh` 로 해결됐다. 이 항목은 그 1회차이므로
  더 셀 것이 없다.
- **회차**: 1 (2회차에서 해결됨)

## 2026-08-07 — R1-3 예외 조항이 "커밋 보류" 를 말하는데 커밋된다

- **어디**: `plugins/harness-core/declarative/rules/core/workflow.md:20` (R1-3)
- **무슨 일**: 규칙은 *"단일 파일 typo · 한 줄 수정 · 임시 탐색은 PR 단위가
  아니다. 결과만 보고하고 commit·push 는 보류"* 라고 적혀 있다. 한 줄 수정
  과제를 준 벤치마크 3회에서 에이전트는 **브랜치는 안 팠지만(예외 준수) 커밋은
  했다(예외 위반)**. 예외의 앞 절반만 지켜진 셈이다.
- **왜 그럴 수 있나**: 같은 절의 불변량이 "`main` 직접 **push** 금지" 로 두 번
  다 *push* 를 말한다. commit 만 하는 요청에는 걸리지 않는 문구다.
- **왜 지금 안 고치나**: 1회차이고, 관측 조건이 "커밋해줘" 라고 명시적으로
  지시한 경우였다 — 사용자 지시가 규칙의 보류 권고를 이긴 것이라면 정상 동작에
  가깝다. 지시 없이도 커밋하는 사례가 나오면 2회차로 센다.
- **2026-08-08 분류**: 대기. 재발 없음.
- **회차**: 1

## 2026-08-07 — `ask` 티어가 헤드리스에서 PR 플로우를 도달 불가로 만든다

- **어디**: `plugins/harness-core/declarative/settings-fragment.json` — `permissions.ask` 의 `Bash(git push:*)`
- **무슨 일**: 비대화형 `claude -p` 세션은 권한 프롬프트에 답할 수 없어 push 가
  항상 거부된다. `acceptEdits` · `dontAsk` · `bypassPermissions` 세 모드 모두에서
  재현했다. `pr-create` 스킬은 Step 5 가 push 라서 헤드리스에서는 **완주가
  구조적으로 불가능**하다.
- **왜 지금 안 고치나**: 1회차이고, `ask` 티어 자체는 의도된 설계다 (되돌리기
  어려운 동작은 사람이 승인한다). 답이 자명하지 않다 — 헤드리스를 감지해
  완화할지, `pr-create` 가 push 직전에 멈추고 명령을 내어줄지, 아니면 문서로만
  알릴지. 2회차가 나오면 셋 중 하나를 고른다.
- **2026-08-08 분류**: 대기. 재발 없음 — 이번 세션의 헤드리스 실행(벤치 72세션)은
  push 를 하지 않아 이 경계에 닿지 않았다.
- **회차**: 1

## 2026-08-07 — `--with` 를 반복하면 앞엣것이 조용히 사라졌다

- **어디**: `plugins/harness-core/bin/harnessctl:54` (인자 파싱)
- **무슨 일**: 사용법은 `--with dev,research` 인데, `--with dev --with research`
  라고 쓰면 뒤엣것이 앞엣것을 덮어써서 **사용자가 명시적으로 요청한 모듈이 경고
  없이 빠졌다.** 문서 정리 중 실제로 이 형태로 쳤다가 발견했다.
- **1회차인데 왜 바로 고쳤나**: 이건 후보 기능이 아니라 **조용한 오동작** 이다.
  §7 의 "두 번 발생을 기다린다" 는 새 규칙·새 훅을 늘릴 때의 기준이지, 이미
  배포된 코드가 요청을 말없이 무시하는 경우에 적용할 기준이 아니다. 반복 시
  누적하도록 고치고 `verify-install.sh` 에 회귀 케이스 둘을 박았다 (90 → 92).
- **회차**: 1 (해결됨)

## 2026-08-07 — 시크릿 픽스처가 GitHub push protection 에 걸린다

- **어디**: `evals/incidents.sh` · `plugins/harness-core/scripts/verify-secret-scrubber.sh`
- **무슨 일**: 저장소를 public 으로 올리는 첫 푸시가 거부됐다. GitHub 이 우리
  Slack 토큰 **픽스처** 를 진짜 시크릿으로 판정했다. 시크릿 탐지기를 시험하려면
  시크릿처럼 생긴 문자열이 있어야 하는데, 그게 정확히 스캐너가 찾는 것이다.
- **고친 방법**: 우리 정규식(`xox[abprs]-` + 20자)에는 걸리고 GitHub 의 구조
  판정에는 안 걸리는 값으로 교체. 훅 차단(exit 2)과 벤치마크 27/29 는 그대로다.
- **일반화**: 새 시크릿 패턴을 추가할 때마다 픽스처가 실제 스캐너에 걸리는지
  확인해야 한다. private 일 때는 드러나지 않는 종류의 문제다.
- **회차**: 1 (해결됨)

## 2026-08-07 — 플러그인 `bin/` 은 사용자 셸에서 안 보인다

- **어디**: `install.sh` · `README.md` (설치 안내)
- **무슨 일**: 사용자가 터미널(zsh)에서 `harnessctl` 을 쳤는데 없었다. 플러그인
  `bin/` 은 Claude Code **Bash 도구** 의 PATH 에만 올라간다. README 는 그냥
  "PATH 에 자동으로 오른다" 고만 적어 두어 사용자가 터미널에서 칠 수 있는 것처럼
  읽혔다 — 설치가 깨진 것처럼 보인다.
- **왜 심링크로 못 푸나**: 캐시가 버전별 디렉터리(`harness-core/1.6.0/bin/`)라
  업데이트마다 경로가 바뀐다. 지금 거는 심링크는 다음 업데이트에 죽는다.
- **고친 방법**: `~/.local/bin/harnessctl` 에 실행 시점 해석 shim. 평범한
  실행 파일이라 셸 무관하고, `sort -V` 로 최신 버전을 고르므로 업데이트를 따라간다.
  PATH 에 없으면 `$SHELL` 을 보고 맞는 rc 파일과 한 줄을 안내한다.
- **1회차인데 왜 바로 고쳤나**: 사용자가 첫 명령에서 막혔다. 설치기가 "완료" 라고
  말한 뒤 안내한 명령이 동작하지 않는 것은 후보 기능이 아니라 결함이다.
- **회차**: 1 (해결됨)

## 2026-08-07 — 벤치가 모델·effort 를 상속하고 기록하지 않았다

- **어디**: `scripts/bench-{convention,lsp}.sh` · `scripts/bench-trigger.py`
- **무슨 일**: `claude -p` 에 `--model`·`--effort` 를 안 넘겨 호출자의
  `settings.json` 을 그대로 상속했다. 실제로는 `opus[1m]` / `effort=high` 였는데
  **어디에도 기록되지 않았다.** 다른 머신에서 돌리면 다른 설정으로 다른 숫자가
  나오는데 왜 다른지 알 방법이 없다.
- **왜 중요한가**: effort 는 결과를 움직일 개연성이 있다. 추론을 더 하면 하네스
  없이도 규약을 지킬 수 있고, 그러면 raw 팔이 올라가 효과가 작게 나온다. 즉 이
  값을 안 적으면 측정의 유효 범위 자체가 불명확하다.
- **고친 방법**: `_bench-lib.sh` 가 `BENCH_MODEL`(기본 opus)·`BENCH_EFFORT`(기본
  high)를 정의하고 세 벤치가 전부 넘긴다. 시작할 때 출력하고 문서에도 명시했다.
- **1회차인데 왜 바로 고쳤나**: 기록되지 않은 설정은 이미 발표된 수치의 유효
  범위를 불명확하게 만든다. 문서가 거짓을 말하는 상태에 가깝다.
- **회차**: 1 (해결됨)

## 2026-08-07 — `verify-benches` 가 코퍼스를 실제보다 작게 센다

- **어디**: `scripts/verify-benches.sh:36`
- **무슨 일**: 게이트가 `grep -c '^inc '` 로 세어 **48** 을 출력하는데, 코퍼스는
  53 이다 (`inc_tool` 5줄이 접두사 `inc ` 에 안 걸린다). `bench-guards.sh` 자신은
  "corpus: 53 cases" 라고 출력하므로 **같은 코퍼스를 두 스크립트가 다르게 센다.**
  `>= 40` 게이트는 통과하니 아무것도 실패하지 않는다.
- **왜 신경 쓰나**: §4b 의 "계측기를 여섯 번 틀렸다" 와 같은 부류다 — 계기가
  조용히 어긋나 있고 화면에는 PASS 로 보인다. 지금은 여유가 커서 무해하지만,
  `inc_tool` 케이스만 늘리면 게이트가 실제 크기와 무관해진다.
- **고친 방법**: `grep -cE '^inc(_tool)? '`. 이제 게이트와 벤치가 같은 53 을
  말한다. 사용자가 같은 턴에 승인해서 바로 고쳤다 — 새 규칙을 늘리는 것이
  아니라 이미 있는 계기가 어긋난 경우라 §7 의 "두 번째 발생을 기다린다" 대상이
  아니다.
- **회차**: 1 (해결됨)

## 2026-08-07 — 문서가 가리키는 내부 참조는 아무도 안 본다 (2회차)

- **어디**: `README.md` — 새로 쓴 `[근거와 방법은 아래](#2-make-bench----날것-…)`
- **무슨 일**: 절 앵커를 손으로 추정해서 적었고 틀렸다. GitHub 의 slug 규칙은
  공백을 접지 않아서 em dash 자리에 하이픈이 둘 남는데 나는 넷을 적었다.
  링크는 렌더링되고, 눌러야만 아무 데도 안 간다는 걸 안다. 임시로 짠 python
  검사기가 잡았고, **그 검사기는 저장소에 남아 있지 않다** — 이번 세션에서
  같은 걸 세 번 다시 썼다.
- **왜 2회차인가**: 위의 `pr-create/SKILL.md:90` 경로 오타(`.claece/`) 와 같은
  부류다. 그 항목이 이미 일반화를 적어 두었다 — *"훅은 verify-*.sh 가 지키고
  프론트매터는 verify-frontmatter.sh 가 지키는데, 본문 안의 경로·명령은 아무도
  안 본다."* 1회차는 스킬 본문의 파일 경로, 2회차는 README 의 절 앵커. 둘 다
  깨져도 에러가 아니라 **아무 일도 안 일어나는 것** 으로 나타난다.
- **R3 criterion**: ≥2회 ✅ · project-agnostic ✅ (자기 파일과 자기 절을 가리키는
  문서는 어느 저장소에나 있다)
- **고친 방법**: `scripts/verify-doc-refs.sh` — 링크 대상 파일 존재, `#anchor` 의
  heading 해석(GitHub slug 규칙), instruction 파일이 부르는 경로의 첫 세그먼트
  존재. `make verify` 에 편입. 자기 케이스 19개를 먼저 돌린다.
- **첫 실행이 3회차를 바로 찾았다**: `pr-review` 와 `research-notes` 본문이
  체크리스트를 `rules/harness/…` 로 가리키고 있었다 (`pr-create` 는 같은 자리를
  `.claude/rules/harness/…` 로 적는다). 프로젝트 루트에서 해석 안 되는 경로다.
- **그리고 오탐 셋을 먼저 냈다** — 동결 코퍼스, `declarative/` 페이로드의
  설치 후 기준 상대 링크, 그리고 **이 원장이 깨진 링크를 증거로 인용한 것**.
  셋 다 검사기를 고쳐서 없앴다. 문서를 고치는 쪽으로 갔으면 맞는 것을 틀리게
  만들었을 것이다.
- **회차**: 2 (해결됨)

## 2026-08-07 — §6 의 "별도 PR" 에 임계값이 없다

- **어디**: `CLAUDE.md` §6 — *"하네스 자체의 구조 변경과 콘텐츠 추가는 별도 PR 로"*
- **무슨 일**: 이번 PR 에 README 개편(문서)과 `verify-benches.sh` 4줄 수정(계기)이
  같이 들어갔다. 문자 그대로 읽으면 PR 둘이어야 하는데, 4줄짜리 게이트 수정에
  PR 을 따로 여는 것은 규칙이 막으려던 문제(리뷰어가 둘 다 제대로 못 본다)를
  일으키지 않는다. 커밋 분리로 갈음하고 PR 본문에 왜 그랬는지 적었다.
- **무엇이 빠졌나**: 규칙에 "언제 커밋 분리로 충분한가" 가 없다. 판단을 매번
  즉석에서 한다.
- **왜 지금 안 고치나**: 1회차. 그리고 임계값을 잘못 적으면 규칙이 오히려
  느슨해진다 — 두 번째 사례를 보고 실제 경계가 어디였는지 확인하고 적는다.
- **2026-08-08 분류**: 대기. 이번 W1 PR 은 스크립트+Makefile+문서를 한 PR 에 담았지만
  **한 작업단위 안의 산출물** 이라 §6 이 막으려는 "구조 변경과 콘텐츠 추가의 혼합" 이
  아니다. 2회차로 세지 않는다.
- **회차**: 1

## 2026-08-07 — verify 총계가 문서 세 곳에 손으로 박혀 있다

- **어디**: `README.md` (배지 · 검증 표 합계) · `docs/agent-layer.md` §4 "현재" 문장
- **무슨 일**: `verify-doc-refs` 를 더하면서 합계를 356 → 425 로 바꿨는데, 그
  숫자가 **세 군데** 에 하드코딩돼 있어 셋을 손으로 맞췄다. 하나만 놓쳤어도
  문서는 조용히 거짓이 되고, `make verify` 는 여전히 통과한다.
- **닮은 사건이 이미 있다**: §4b 의 *"`bench-claims` 가 조용히 썩었는데 README 는
  마지막 수치를 현재형으로 인용하고 있었다"*. 기제는 다르다 (그건 벤치가 빈
  결과를 냈고, 이건 복제된 상수다) — 같은 항목으로 세지 않는다. 다만 이 저장소가
  `check-claims.sh` 로 **슬라이드** 에서 막는 바로 그 문제를 자기 README 에서는
  안 막고 있다는 점은 적어 둔다.
- **왜 지금 안 고치나**: 1회차. 그리고 같은 세션에서 검사기를 하나 막 만들었다 —
  1회차에 검사기를 또 붙이는 것이 §7 이 경고하는 "혹시 몰라서" 그 자체다.
  2회차가 나오면 `make verify` 의 실제 합과 문서의 숫자를 대조하는 검사가 답이다.
- **2026-08-08 — 2회차 발생.** `context-budget` 을 더하면서 425 → 426 을 **또 세
  군데에 손으로** 박았다 (README 배지 · README 표 · agent-layer §4). 예고한 조건이
  그대로 왔다. 아이러니는 같은 PR 이 *컨텍스트* 총계는 손 계산에서 떼어냈다는
  것이다 — **검사 총계는 그대로 뒀다.**
- **제안**: `make verify` 가 실제로 낸 합과 문서에 적힌 숫자를 대조하는 검사.
  `context-budget.sh` 와 같은 모양이고, 세 자리를 한 자리로 줄인다.
  **하네스 변경이므로 별도 PR** (§6).
- **2026-08-08 — 3회차.** 문서를 하나 더하자 `verify-doc-refs` 의 파일 수가 50 → 51
  이 되어 426 → 427 을 **또 세 군데에 손으로** 박았다. 이제 이 항목은 제안이 아니라
  밀린 일이다.
- **2026-08-08 — 4회차.** ADR-0012 를 더하자 427 → 428 을 또 세 군데에서 손으로
  고쳤다. 네 번이면 더 셀 이유가 없다.
- **고친 방법**: `scripts/verify-check-total.sh` + `make verify-all`. 두 가지를
  차례로 묻는다 — 발행된 셋이 서로 맞는가, 그리고 그 숫자가 **실제 실행 결과와**
  맞는가. 계수 규칙을 스크립트 헤더에 적어 두어 반박할 수 있게 했다. CI 의 ubuntu
  job 이 `verify` 대신 이것을 부른다.
- **회차**: 4 (해결됨)

## 2026-08-07 — 저장소 전용 검증기의 케이스 의무가 규정에 없다

- **어디**: `CLAUDE.md` §4 (검증 의무) · `docs/agent-layer.md` §4
- **무슨 일**: §4 는 **훅** 에 "검증기 하나, 8케이스 이상, 세 종류" 를 요구하고
  스킬에는 트리거 eval 을 요구한다. 그런데 `scripts/` 의 저장소 전용 검증기에
  대한 규정은 없다 — `verify-frontmatter.sh` 는 자기 케이스가 없고,
  `verify-doc-refs.sh` 는 19개를 넣었다. 둘 다 규정을 어기지 않았다는 뜻이다.
- **판단 근거는 있었다**: doc-refs 의 유일한 실패 방식이 오탐이고, 오탐 내는
  린터는 꺼진다. frontmatter 는 참/거짓이 명확하다. 즉 "케이스가 필요한가" 는
  검증기의 실패 방식이 정하는데, 그 기준이 어디에도 안 적혀 있다.
- **왜 지금 안 고치나**: 1회차이고, 규정을 먼저 적으면 두 사례만 보고 일반화하는
  것이 된다. 세 번째 저장소 전용 검증기를 만들 때 실제 경계를 보고 적는다.
- **2026-08-08 — 세 번째가 생겼다.** `scripts/context-budget.sh`. 또 즉석에서
  판단했고, 자기 케이스를 넣지 않았다. 근거는 있었다 — 실패 방식이 *오탐* 이
  아니라 *누락* 이고, 파일 목록을 하드코딩 대신 glob 으로 두면 누락이 구조적으로
  불가능해진다. 즉 **케이스 대신 설계로 막았다.**
- **세 사례가 만든 기준** (이제 적을 수 있다): 검증기의 실패 방식이
  **오탐**이면 케이스를 넣는다 (오탐 내는 검사는 꺼진다 — `verify-doc-refs`).
  **누락**이면 먼저 설계로 막고, 못 막을 때만 케이스를 넣는다 (`context-budget`
  의 glob). 참/거짓이 자명하면 둘 다 불필요하다 (`verify-frontmatter`).
- **제안**: 위 세 줄을 `CLAUDE.md` §4 에 넣는다. **하네스 변경이므로 별도 PR** (§6).
- **회차**: 2 — 제안함, 승인 대기

## 2026-08-07 — 스킬과 그 SOT 가 서로 다른 스코프로 배포된다

- **어디**: `install.sh` (기본 `--scope user`) · `plugins/harness-core/bin/harnessctl:207`
  (user 스코프에서 `rules/` 를 건너뜀) · `plugins/harness-dev/skills/pr-review/SKILL.md:50`
- **무슨 일**: `pr-review` Step 4 는 *"`review.md` R2 의 6 항목을 순서대로
  걸어간다"* 라고 쓴다 — 체크리스트가 스킬 본문에 없고 rule 파일에 있다. 그런데
  **rule 은 프로젝트 스코프에만 설치되고, 설치기의 기본 스코프는 user 다.**
  즉 README 가 "가장 흔한 조합" 으로 안내하는 `./install.sh --profile dev,python`
  을 그대로 따르면 스킬은 전역에 깔리고 그 SOT 는 아무 데도 안 깔린다.
- **이 머신에서 확인**: `~/.claude/rules/` 없음. `harness-dev` · `harness-research`
  플러그인은 user 스코프로 설치돼 스킬이 살아 있는데, `review.md` 와 `notes.md`
  는 이 머신 어디에도 없다.
- **왜 설계가 그런가는 안다**: `~/.claude/rules` 는 읽히는 자리가 아니라서 거기
  두면 "동작하는 얼굴로 죽어 있는 파일" 이 된다 (harnessctl 주석). 문제는 그
  판단이 옳다는 것이 아니라, **그 결과로 생기는 구멍을 아무도 말해주지 않는다** 는
  것이다. `doctor` 는 "이상 없음" 이라고 말한다.
- **가장 싼 답부터**: `doctor` 가 "dev/research 플러그인이 있는데 대응 rule 이
  대상 스코프에 없다" 를 한 줄로 알리는 것. 스킬 본문에 체크리스트를 복제하는
  것은 SOT 두 벌이 되므로 (ADR-0004) 답이 아니다.
- **왜 지금 안 고치나**: 1회차. 다만 이건 후보 기능이 아니라 **안내가 침묵하는
  구멍** 이라, 2회차가 나오면 바로 `doctor` 에 넣는다.
- **2026-08-08 분류**: 대기. 재발 없음 — 다만 이번에 컨텍스트 예산을 재면서
  `rules/` 가 프로젝트 스코프 전용이라는 사실이 §4 표와 README 에 수치로
  드러났으므로, 구멍의 크기는 이제 문서화돼 있다 (user 3.9k 대 project 8.0k).
- **회차**: 1

## 2026-08-08 — PR 제목 70자 상한을 아무것도 재지 않는다 (2회차)

- **어디**: `plugins/harness-core/declarative/rules/core/workflow.md` R4 체크리스트 ·
  `plugins/harness-core/skills/pr-create/SKILL.md` Step 5
- **무슨 일**: R4 는 *"PR title 이 `[<slug>] <description>` 형식이고 70자 이하"* 를
  체크 항목으로 두는데, **재는 것이 없다.** 사람도 에이전트도 눈으로 센다.
  - **1회차**: PR #4 `[bench-trigger-project-skills] Measure skills…` — **71자**로
    나갔고 **그대로 머지됐다.** 아무도 몰랐다.
  - **2회차**: PR #6 을 **79자**로 열었다. `gh pr view --json title` 로 다른 걸
    보다가 우연히 걸렸다.
- **왜 조용한가**: 훅은 `git commit` 을 보고 `ai-attribution-guard` 는 커밋·PR
  명령을 보는데, **길이는 아무 가드의 관심사가 아니다.** 그리고 PR 제목은
  저장소 파일이 아니라 `make verify` 계열이 닿지 못한다.
- **제안**: `pr-create` 스킬 Step 5 가 `gh pr create` 를 부르기 **전에** 제목
  길이를 계산해 넘으면 멈춘다. 검증기가 아니라 스킬 본문이 자리인 이유는, 제목이
  존재하는 유일한 순간이 거기이기 때문이다. 훅으로 만들려면 `gh pr create` 의
  `--title` 을 파싱해야 하는데 인용 형태가 여러 가지라 취약하다.
- **왜 지금 안 고치나**: 하네스 변경이라 별도 PR 이다 (§6). 그리고 이 PR 은
  *빼는 작업* 이라 스킬 본문 변경을 섞지 않는다.
- **2026-08-08 — 3회차.** PR #9 를 **71자**로 열었다. 이 항목을 2회차로 적어둔
  바로 그 세션에서, 적은 지 몇 시간 만이다. **사람이 세는 규칙은 사람이 잊는다는
  것의 증거로 이보다 나은 것이 없다.**
- **고친 방법**: `pr-create` Step 5 가 `gh pr create` 직전에 `${#TITLE}` 를 세고
  넘으면 멈춘다. 검증기가 아니라 스킬 본문에 둔 이유는 **제목이 존재하는 유일한
  순간이 거기** 이기 때문이다 — PR 제목은 저장소 파일이 아니라 `make verify` 가
  닿지 못한다.
- **회차**: 3 (해결됨)

## 2026-08-08 — allow 티어에 검증 러너가 하나뿐이라 §4 를 지킬 수 없다

- **어디**: `plugins/harness-core/declarative/settings-fragment.json` — `permissions.allow`
- **무슨 일**: `CLAUDE.md` §4 는 *"run it, look at what came out"* 을 요구하고,
  새 R6 은 *"성공 기준을 실행 가능한 명령으로 적고 실행한다"* 를 요구한다. 그런데
  allow 42항목 중 **러너는 `Bash(make:*)` 하나뿐이다** — `python3`·`pytest`·`npm`·
  `sh`·`./script` 전부 없다. 즉 Makefile 이 없는 저장소에서는 **하네스가 명령한
  검증이 매번 승인 프롬프트를 띄우고, 헤드리스에서는 아예 불가능하다.**
- **어떻게 드러났나**: R6 벤치마크 1차 파일럿이 양팔 0/3 으로 나왔다. 스트림을
  뜯어보니 에이전트는 `sh check.sh` 를 **요청했고 거부됐다.** 즉 0/3 은 규칙에
  대한 증거가 아니라 권한에 대한 증거였다.
- **시험 대상 에이전트가 스스로 이 gap 을 보고했다** — `*harness gap*:
  .claude/settings.json:permissions.allow …` 한 줄로. 그리고 **원장에 적지도
  못했다**: `.claude/` 쓰기도 승인이 필요해 거부됐다. §5 의 "같은 턴에 기록하라"
  와 충돌하는 두 번째 지점이다.
- **경계가 이미 일관되지 않다**: `make` 는 허용돼 있는데 Makefile 은 임의 코드를
  실행한다. 즉 "테스트 러너는 임의 실행이라 위험하다" 는 선은 이미 넘어 있고,
  `pytest` 만 막혀 있다.
- **왜 2회차로 세나**: 원장의 *"`ask` 티어가 헤드리스에서 PR 플로우를 도달 불가로
  만든다"* 와 **같은 부류의 두 번째 발생** 이다 — 권한 설계가 하네스 자신이
  명령한 단계를 도달 불가로 만든다. 그쪽은 `ask`, 이쪽은 `allow` 의 누락이다.
- **제안**: allow 에 좁은 러너 항목을 더한다 — `Bash(pytest:*)` ·
  `Bash(python3 -m pytest:*)` · `Bash(npm test:*)` · `Bash(cargo test:*)` ·
  `Bash(go test:*)`. 임의 `python3`·`sh` 는 넣지 않는다 (그건 셸을 여는 것이다).
  **권한 변경이므로 승인 대상이고 별도 PR 이다.**
- **우회는 하지 않았다.** 벤치마크 픽스처를 `make check` 로 바꿔 지금 측정 가능하게
  했지만, 그건 계측기를 허용된 러너에 맞춘 것이지 gap 을 고친 것이 아니다.
- **회차**: 2 — 제안함, 승인 대기
## 2026-08-08 — 하네스가 "사용자에게 어떻게 보고하는가" 를 아예 다루지 않는다

- **어디**: `plugins/harness-core/declarative/CLAUDE.md` — §1~5 어디에도 없음
- **무슨 일**: 5원칙은 *코드를 어떻게 쓰는가* 만 다룬다. 사용자에게 나가는 보고의
  품질 — 자연스러운 한국어인가, 처음 보는 용어를 설명했는가, 구체적인가 — 은
  규약에도 가드에도 없다. 사용자가 외부 제안서를 들고 와 지적했다.
- **증거는 이 세션 안에 있다**: 내 보고가 `MDE 61%`, `짝비교`, `negative routing`,
  `2회차`, `§4b` 같은 표현을 설명 없이 쓴다. 이 저장소를 아는 독자에게는 효율적이고
  모르는 독자에게는 아무 정보가 아니다. **누가 독자인지 규약이 정해두지 않았으므로
  둘 중 어느 쪽으로도 틀렸다고 말할 근거가 없다** — 그것이 gap 이다.
- **제안된 구조**: `CLAUDE.md` 보고 규약 → Stop prompt hook 게이트 → 회귀 스위트.
  이 저장소의 3층 패턴(규약 → 가드 → 검증)과 같은 모양이다.
- **왜 지금 안 만드나**: 1회차이고, 게이트가 **턴마다 모델을 부른다.** 우리는 상시
  컨텍스트만 재고 **턴당 비용은 재는 장치가 없다.** 그리고 §4b 의 "전부 차단하는
  가드는 하루 만에 꺼진다" 가 정확히 이 종류에 걸린다 — 오탐률을 모르는 채로
  매 턴을 막는 가드를 배포할 수 없다.
- **먼저 할 것**: 이 세션의 실제 보고를 코퍼스로 리뷰어 프롬프트를 **오프라인으로**
  돌려 플래그율과 오탐을 센다. `bench-claims` 와 같은 모양이고 턴당 비용이 없다.
- **2026-08-08 — 쟀다.** 이 세션의 보고 40개(손으로 안 고름, 200자 이상 한글 포함)에
  리뷰어를 오프라인으로 돌렸다. 대조군 6/6 으로 계기는 유효했다.
  **플래그 31/40 (77%). 그중 18건이 독자가 소유한 라벨**(`§4b`·`R6`·`2회차`·`SOT`·
  `ADR-N`·`원장`)**을 설명하라는 요구였다** — 자기가 쓴 문서의 절 번호를 자기에게
  설명하라는 것이라 이 독자에게는 오탐이다. 리뷰어는 **독자가 누구인지 모르므로**
  `§4b`(독자의 것)와 `artifact boundary`(에이전트가 지어낸 것)를 못 가른다.
  그리고 리뷰어 자신이 틀리기도 했다 — `'프로브에서'` 를 부자연스럽다며 *"아마
  '프로젝트에서' 를 의도했을 것"* 이라고 단정했다.
- **그래서 게이트는 배포하지 않는다.** 네 턴 중 세 턴이 막히고, 막는 이유의 절반
  이상이 독자 자신의 문서 인용이다. §4b 의 *"전부 차단하는 가드는 하루 만에 꺼진다"*.
  만들려면 **독자 컨텍스트를 입력으로 받아야 한다** — 측정이 밝힌 유일한 설계 결함이다.
- **남은 13건에는 진짜가 있었다**: 지시 대상 없는 숫자(`27/29` 가 무엇을 세는지 없음) ·
  소개 없이 등장한 이름(`체커`) · 앞 턴 참조(`Problem ②`). **그 셋만** `CLAUDE.md` §6
  으로 넣었다 (+277 tok, 여유 974 → 697).
- **회차**: 1 (부분 해결 — 규약은 넣고, 게이트는 근거를 갖고 보류)
